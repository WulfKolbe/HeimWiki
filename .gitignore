# Verbatim third-party source extractions — full text of copyrighted works.
raw/

# Derived, rebuildable. The command behind each is in out/314.txt.
# NOTE: git does not support trailing comments on a pattern line — a comment
# after the pattern becomes part of the pattern and it silently matches nothing.
# ./build/build.sh
build/tw/
# any python3 run
build/__pycache__/
# python3 build/parse_register.py
build/register.json
# scratch; the numbered reports in out/ are tracked
out/*.log

# llmwiki workspace index — rebuilt by `llmwiki init` / `reindex`
.llmwiki/

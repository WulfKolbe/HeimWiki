---
title: "Elementary Structures of Matter, Volume 2 — page 280"
type: page
tags: [bh2, page]
sources: [bh2]
page: 280
lang: EN-US
lang_source: de
---
$=K_{(\gamma \delta)}^{(\alpha \beta)} ;\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} \quad$ is applied. If the $C$ structure of the  charge field is referenced to $(+7)_{d}$ and $r$ is a $R_{3}$ distance from the $(+7)_{d}$ center, such that in (79) and (79a), with $\mu ; n \rightarrow r$ in the third validity region $(+7)_{d} \equiv\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} ; n \rightarrow \psi(r)$ and the approximation $\psi(r) \sim e^{\underline{\lambda} r}\left(\frac{1}{2} \sqrt{\frac{1+b}{1-b}}\left(e^{\lambda r}-b\right)^{2}\right)^{-a /(2 \lambda)} \approx \exp (\underline{\lambda}-a) r=e^{-\alpha r} \quad$ developed according to (79a), the approximation $\psi(r) \sim e^{\underline{\lambda} r}\left(\frac{1}{2} \sqrt{\frac{1+b}{1-b}}\left(e^{\lambda r}-b\right)^{2}\right)^{-a /(2 \lambda)} \approx \exp (\underline{\lambda}-a) r=e^{-\alpha r} \quad$ holds; then, in the proportionality factor $A$ of $\psi=A e^{-\alpha r}$ and in $\alpha>0$ contain the observable properties of $(+7)_{d} \rightarrow \psi(r)$ of the $d$ structure. Because of this exponential behavior, one could approximately assume $s p K_{(\gamma \delta)}^{(\alpha \beta)} ;\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} ; n \rightarrow F_{2}-F_{1}+G_{2}-G_{1}$ for the $s p K_{(\gamma \delta)}^{(\alpha \beta)} ;()$-determining summands, if, due to the energetic nature of the condensation stages, $F_{2}-F_{1}+G_{2}-G_{1}>0$ holds. If we speculatively identify $\dot{F_{1,2}}=V_{1,2}$ and $G_{1,2}=Q_{1,2}$, then $V_{1}+Q_{1}>V_{2}+Q_{2}$ or, after substituting these ratios, $\eta_{q k}^{-1}<\left(\frac{1+\sqrt{\eta_{q}}}{2 \sqrt{\eta \eta_{q}}}\right)^{2}+\left(1-\frac{\eta_{q}}{\eta}\right) \sqrt{\eta}$ would hold. Using (98), this yields the inequality $k<u_{q}=\left(\frac{\pi}{q}\right)^{4}\left[\left(\left(\frac{1+\sqrt{\eta_{q}}}{2 \sqrt{\eta \eta_{q}}}\right)^{2}+\left(1-\frac{\eta_{q}}{\eta}\right) \sqrt{\eta}\right)^{4}-1\right]-4$ as a condition on the values of $k$ and $q$. Numerical verification yields $2<u_{q}<3$ for $q=1$ and $q=2$ as well as $1<u_{3}<2$ for $q=3$,  but $0<u_{4}<1$ for $q=4$ and $u_{q}<0$ for all $q>4$. Assuming that this more speculative view is correct, then only $k=1$ and $k=2$ from $q=1$ to $q=3$ would be compatible with (72), so that (72) would need to be supplemented by

<!--okf:source lang="de"-->
> **Original**
>
> $=K_{(\gamma \delta)}^{(\alpha \beta)} ;\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} \quad$ angewendet werden. Wird die $C$-Struktur des  Ladungsfeldes auf $(+7)_{d}$ bezogen und ist $r$ eine $R_{3}$-Distanz hin sichtlich des $(+7)_{d}$-Zentrums, derart, daß in (79) und (79a) mit  $\mu ; n \rightarrow r$ im dritten Gültigkeitsbereich $(+7)_{d} \equiv\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} ; n \rightarrow \psi(r)$ und  der nach (79a) entwickelten Approximation  $\psi(r) \sim e^{\underline{\lambda} r}\left(\frac{1}{2} \sqrt{\frac{1+b}{1-b}}\left(e^{\lambda r}-b\right)^{2}\right)^{-a /(2 \lambda)} \approx \exp (\underline{\lambda}-a) r=e^{-\alpha r} \quad$ gilt,  dann sind im Proportionalitätsfaktor $A$ von $\psi=A e^{-\alpha r}$ und in $\alpha>0$  die observablen Eigenschaften von $(+7)_{d} \rightarrow \psi(r)$ der $d$-Struktur ent halten. Wegen dieses exponentiellen Verlaufes könnte man approxima tiv $s p K_{(\gamma \delta)}^{(\alpha \beta)} ;\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} ; n \rightarrow F_{2}-F_{1}+G_{2}-G_{1}$ für die $s p K_{(\gamma \delta)}^{(\alpha \beta)} ;()$ bestim menden Summanden annehmen, wenn wegen der energetischen Natur  der Kondensationsstufen $F_{2}-F_{1}+G_{2}-G_{1}>0$ ist. Wird spekulativ  die Identifikation $\dot{F_{1,2}}=V_{1,2}$ und $G_{1,2}=Q_{1,2}$ vorgenommen, dann  würde $V_{1}+Q_{1}>V_{2}+Q_{2}$ oder nach Substitution dieser Verhältnisse  $\eta_{q k}^{-1}<\left(\frac{1+\sqrt{\eta_{q}}}{2 \sqrt{\eta \eta_{q}}}\right)^{2}+\left(1-\frac{\eta_{q}}{\eta}\right) \sqrt{\eta}$ gelten. Mit (98) folgt daraus die Un gleichung $k<u_{q}=\left(\frac{\pi}{q}\right)^{4}\left[\left(\left(\frac{1+\sqrt{\eta_{q}}}{2 \sqrt{\eta \eta_{q}}}\right)^{2}+\left(1-\frac{\eta_{q}}{\eta}\right) \sqrt{\eta}\right)^{4}-1\right]-4$  als Bedingung der $k$ - und $q$-Werte. Die numerische Überprüfung lie fert $2<u_{q}<3$ für $q=1$ und $q=2$ sowie $1<u_{3}<2$ für $q=3$,  aber $0<u_{4}<1$ für $q=4$ und $u_{q}<0$ für alle $q>4$. Nimmt man  an, daß diese mehr spekulative Betrachtung richtig ist, dann wäre nur  $k=1$ und $k=2$ von $q=1$ bis $q=3$ mit (72) verträglich, so daß  (72) zu ergänzen wäre durch
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 280.

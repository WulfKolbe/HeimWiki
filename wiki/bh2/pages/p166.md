---
title: "Elementary Structures of Matter, Volume 2 — page 166"
type: page
tags: [bh2, page]
sources: [bh2]
page: 166
lang: EN-US
lang_source: de
---
as a compositional conservation principle of the space compressor. This conservation principle states that the state of compression and condensation described by $\left(()-\frac{1}{2}{ }^{2} \bar{\gamma} s p()\right) ; s p^{4} \bar{\varrho}$ is always conserved in the compositional field, regardless of symmetrical changes in the partial structures. 

For brevity, $\alpha \hat{=}(\mu \nu), \beta \hat{=}(\kappa \lambda)$ and $\left[\begin{array}{c}\widehat{\mu v} \\ -\quad+ \\ \kappa \lambda\end{array}\right] \hat{=}\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]$ will be used in what follows. The coupling extrema $\partial Q_{(\beta)}^{(\alpha)}=0$  characterize coupling classes I $a$ and $\mathrm{II} a$ via $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\hat{0}$,  which represents a minimum value of the condenser. However, according to Equations (63) and (64), $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\hat{0}$ implies $\psi_{(\beta)_{k l}}^{(\alpha)}=0$, i.e., $\left(\psi_{k l}\right)^{\underline{\alpha}_{k l}}=0$. However, the  particular integral of the composition field is defined, after factoring out the eigenvalues of the condensation stages, in the open interval $0<\psi_{k l}<E$, so that $\left(\psi_{k l}\right)^{\underline{\alpha}_{k l}}=0$ can only be satisfied for $\underline{\alpha}_{k l} \rightarrow-\infty$, or for $\psi_{k l}=0$. In any case, this describes $\partial Q_{(\beta)}^{(\alpha)}=0$ with $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\hat{0}$ as the coupling maximum, within whose domain of definition $\mu ; n_{-}=N_{-}$the lattice and hyperselectors become proportional to one another. Accordingly, there must be another point in the same domain of definition, namely $\mu ; n_{+}=$ $=N_{+}$, in which $\delta\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\widehat{0}$ satisfies an extremum condition, and this extremum $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right](+) \neq \hat{0}$ can only be a condensation maximum. Since the coupling maxima coincide with the condensation minima, it follows that the condensation maxima in $N_{+}$are associated with coupling minima, in which, however, the extremal condition $\partial Q_{(\beta)}^{(\alpha)}=0$ must also be satisfied. This relationship, in turn, provides  information about the location of $N_{+}$; for it follows that $0=\delta\left[\begin{array}{c}i \\ k l\end{array}\right]_{(\beta)}^{(\alpha)}+$

<!--okf:source lang="de"-->
> **Original**
>
> als ein kompositives Erhaltungsprinzip des Raumkompressors. Dieses  Erhaltungsprinzip sagt aus, daß der durch $\left(()-\frac{1}{2}{ }^{2} \bar{\gamma} s p()\right) ; s p^{4} \bar{\varrho}$  beschriebene Kompressions- und Kondensationszustand im Komposi tionsfeld immer erhalten bleibt und zwar unabhängig von synmetroni schen Änderungen der Partialstrukturen. 
>
> Im folgenden sollen zur Kürzung $\alpha \hat{=}(\mu \nu), \beta \hat{=}(\kappa \lambda)$ und  $\left[\begin{array}{c}\widehat{\mu v} \\ -\quad+ \\ \kappa \lambda\end{array}\right] \hat{=}\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]$ verwendet werden. Die Kopplungsextrema $\partial Q_{(\beta)}^{(\alpha)}=0$  charakterisieren die Kopplungsklassen I $a$ und $\mathrm{II} a$ durch $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\hat{0}$,  was einen Minimalwert des Kondensors darstellt. $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\hat{0}$ bedingt  aber nach Gleichung (63) und (64) $\psi_{(\beta)_{k l}}^{(\alpha)}=0$, also $\left(\psi_{k l}\right)^{\underline{\alpha}_{k l}}=0$. Das  parktikuläre Integral des Kompositionsfeldes ist aber nach Ausklam merung seiner Eigenwerte der Kondensationsstufen im offenen Inter vall $0<\psi_{k l}<E$ definiert, so daß $\left(\psi_{k l}\right)^{\underline{\alpha}_{k l}}=0$ nur für $\underline{\alpha}_{k l} \rightarrow-\infty$  erfüllt werden kann, oder für $\psi_{k l}=0$. In jedem Fall wird hierdurch  aber $\partial Q_{(\beta)}^{(\alpha)}=0$ mit $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\hat{0}$ als Kopplungsmaximum beschrieben, in  dessen Definitionsbereich $\mu ; n_{-}=N_{-}$Gitter- und Hyperselektoren  einander proportional werden. Ganz entsprechend muß es einen  anderen Punkt im gleichen Definitionsbereich geben, nämlich $\mu ; n_{+}=$  $=N_{+}$, in welchem $\delta\left[\begin{array}{c}\alpha \\ \beta\end{array}\right]=\widehat{0}$ einer Extremalbedingung genügt, und  dieses Extremum $\left[\begin{array}{c}\alpha \\ \beta\end{array}\right](+) \neq \hat{0}$ kann nur ein Kondensationsmaximum  sein. Da sich die Kopplungsmaxima mit den Kondensationsminima  decken, folgt, daß die Kondensationsmaxima in $N_{+}$mit Kopplungs minima verbunden sind, in denen aber ebenfalls die Extremalbedin gung $\partial Q_{(\beta)}^{(\alpha)}=0$ erfüllt sein muß. Diese Beziehung wiederum liefert  eine Aussage über die Lage von $N_{+}$; denn es folgt $0=\delta\left[\begin{array}{c}i \\ k l\end{array}\right]_{(\beta)}^{(\alpha)}+$
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 166.

---
title: "Elementary Structures of Matter, Volume 2 — page 99"
type: page
tags: [bh2, page]
sources: [bh2]
page: 99
lang: EN-US
lang_source: de
---
the form $\frac{\alpha_{\tilde{k}}}{\alpha_{i}} \boldsymbol{S} \partial_{i} \gamma_{\tilde{k} l}^{(p q)} ; n \partial n^{\tilde{k}}=\frac{1}{\alpha_{i}} \partial_{i} A_{l}^{(p q)} ; n$, that is, $\gamma_{-i l}^{(p q)}=$  $=\frac{1}{\alpha_{i}} \partial_{i} A_{l}^{(p q)}-\frac{1}{\alpha_{l}} \partial_{l} A_{i}^{(p q)}=\left(\operatorname{ROT}_{(x)} \bar{A}_{(p q)}\right)_{i l}$ or ${ }^{2} \bar{\gamma}_{(p q)-}=\operatorname{ROT}_{(x)} \bar{A}_{(p q)}$. Since the $\alpha_{l}$ are not dimensioned and differ qualitatively from one another depending on whether $l \leqq 3$ or $l>3$ , the metronic  grid $C$ must be the metronomic field selector according to III. and IV., 1. must be adapted to these specific conditions of the empty reference world $R_{6(0)}$.  During this adaptation, ${ }^{2} \bar{F}=\boldsymbol{S} \boldsymbol{S} \delta \bar{s} \times \delta \bar{\sigma}$ gives rise to the equivalent ${ }^{2} \bar{F}=\operatorname{ROT}_{(x)} \bar{\phi} ; n$, thereby allowing an equivalent spin field selector $\tau^{2} \bar{P}=\operatorname{ROT}_{(x)} \bar{\phi}$ to be defined using ${ }^{2} \bar{F}=\tau^{2} \bar{P} ; n$, where the metronic spin field function $\bar{\phi} ; n$ now has the dimension [ $m^{3}$ ] following this dimensional transformation. However, for ${ }^{2} \bar{\gamma}_{(p q)-}$, the selector equation ${ }^{2} \bar{\gamma}_{(p q)-}=\operatorname{ROT}_{(x)} \bar{A}_{(p q)}$ also holds, in which, due to $\tau=$ const, the selector proportionality $\bar{\phi}_{(p q)}=\tau \bar{A}_{(p q)}$ always holds,  that is, $\bar{A}_{(p q)}$ can always be interpreted as a spin field selector.  Substitution yields ${ }^{2} \bar{\gamma}_{(p q)-}=\frac{1}{\tau} \operatorname{ROT}_{(x)} \bar{\phi}_{(p q)}={ }^{2} \bar{P}_{(p q)}$ due to $\operatorname{ROT}_{(x)} \bar{\phi}=$  $=\tau^{2} \bar{P}$ according to III. and IV., 1. However, this means that all correlative field activators are nothing other than spin selectors of the respective partial structures, which—in contrast to the associated Hermitian protofields—can certainly be functions of the anti-Hermitian architectural units. From

<!--okf:source lang="de"-->
> **Original**
>
> die Form $\frac{\alpha_{\tilde{k}}}{\alpha_{i}} \boldsymbol{S} \partial_{i} \gamma_{\tilde{k} l}^{(p q)} ; n \partial n^{\tilde{k}}=\frac{1}{\alpha_{i}} \partial_{i} A_{l}^{(p q)} ; n$, also $\gamma_{-i l}^{(p q)}=$  $=\frac{1}{\alpha_{i}} \partial_{i} A_{l}^{(p q)}-\frac{1}{\alpha_{l}} \partial_{l} A_{i}^{(p q)}=\left(\operatorname{ROT}_{(x)} \bar{A}_{(p q)}\right)_{i l}$ oder  ${ }^{2} \bar{\gamma}_{(p q)-}=\operatorname{ROT}_{(x)} \bar{A}_{(p q)}$ gebracht werden kann. Da die $\alpha_{l}$ nicht undi mensioniert sind und sich qualitativ voneinander unterscheiden, je  nachdem ob $l \leqq 3$ oder $l>3$ ist, muß, bezogen auf das metronische  Gitter $C$, der metronische Spinfeldselektor nach III. und IV., 1. diesen  speziellen Bedingungen der leeren Bezugswelt $R_{6(0)}$ angepaßt werden.  Bei dieser Anpassung entsteht aus ${ }^{2} \bar{F}=\boldsymbol{S} \boldsymbol{S} \delta \bar{s} \times \delta \bar{\sigma}$ das Äquivalent  ${ }^{2} \bar{F}=\operatorname{ROT}_{(x)} \bar{\phi} ; n$, wodurch mit ${ }^{2} \bar{F}=\tau^{2} \bar{P} ; n$ ein äquivalenter Spinse lektor $\tau^{2} \bar{P}=\operatorname{ROT}_{(x)} \bar{\phi}$ definiert werden kann, wo die metronische  Spinfeldfunktion $\bar{\phi} ; n$ nach dieser Dimensionierungstransformation  nunmehr die Dimensionierung [ $m^{3}$ ] trägt. Für ${ }^{2} \bar{\gamma}_{(p q)-}$ gilt aber eben falls die Selektorgleichung ${ }^{2} \bar{\gamma}_{(p q)-}=\operatorname{ROT}_{(x)} \bar{A}_{(p q)}$ in welcher wegen  $\tau=$ const stets die Selektorproportionalität $\bar{\phi}_{(p q)}=\tau \bar{A}_{(p q)}$ existiert,  das heißt, $\bar{A}_{(p q)}$ ist stets durch einen Spinfeldselektor interpretierbar.  Einsetzen liefert ${ }^{2} \bar{\gamma}_{(p q)-}=\frac{1}{\tau} \operatorname{ROT}_{(x)} \bar{\phi}_{(p q)}={ }^{2} \bar{P}_{(p q)}$ wegen $\operatorname{ROT}_{(x)} \bar{\phi}=$  $=\tau^{2} \bar{P}$ nach III. und IV., 1. Dies bedeutet aber, daß alle korrelativen  Feldaktivatoren nichts anderes als Spinselektoren der betreffenden Par tialstrukturen sind, die im Gegensatz zu den zugehörigen hermiteschen  Protofeldern durchaus Funktionen der antihermetrischen Architektur einheiten sein können. Aus
<!--/okf:source-->

field selector is used, which leads to $\tau \alpha_{\tilde{k}}\left(\gamma_{+\tilde{k} m}^{(p q)}+P_{\tilde{k} m}^{(p q)}\right)=\delta_{\tilde{k}} \phi_{m}^{(p q)}$. In this way, the protofield components are also expressed by the metronic spin field selector if these components contain the index of an antihermetic level. In the Metron integral, the constant can always be determined such that it may be set to 0. Substituting

<!--okf:source lang="de"-->
> **Original**
>
> feldselektor verwendet werden, was zu $\tau \alpha_{\tilde{k}}\left(\gamma_{+\tilde{k} m}^{(p q)}+P_{\tilde{k} m}^{(p q)}\right)=\delta_{\tilde{k}} \phi_{m}^{(p q)}$  führt. Auf diese Weise werden also auch die Protofeldkomponenten  durch den metronischen Spinfeldselektor ausgedrückt, wenn diese  Komponenten den Index einer antihermetrischen Stufe enthalten. In  dem Metronintegral kann die Konstante immer so bestimmt werden,  daß für sie der Wert 0 gesetzt werden darf. Einsetzen von
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 99.

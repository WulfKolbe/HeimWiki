---
title: "Elementary Structures of Matter, Volume 2 — page 265"
type: page
tags: [bh2, page]
sources: [bh2]
page: 265
lang: EN-US
lang_source: de
---
may be metronic constants. There must therefore be 4 possible  volume elements $\partial V_{s}=\beta_{s} \prod_{k=1}^{3-s} \partial_{k}()_{k}$ with constant $\beta_{s}=$, which  points to the nature of the real metronic $R_{3}$ cells. Setting this condition thus yields $\partial_{V}{ }^{2} \bar{M}_{(\kappa i)}^{(\mu v)} \sim \bar{\lambda}_{(\kappa \lambda)}^{(\mu v)}\left[\begin{array}{c}\widehat{\mu v} \\ {\left[\begin{array}{c}-\alpha \\ +\end{array}\right]_{+}} \\ \text {+ }\end{array}\right.$, and here the right-hand side is a selector that, according to the synmetronic investigations in VI., 3., describes the condensation state and, consequently, according to VII., 1., and $\bar{\lambda}_{(\kappa \lambda)}^{(\mu v)} \perp \vec{Y}$, the inertial state of a coupling group $( \pm p)_{(\kappa \lambda)}^{(\mu v)}$. This fact becomes particularly clear from the fact that this state is simultaneously interpreted as an energy density—an action density in the $R_{4}$-projection of $R_{6}$  and, for all interactions $w$, the quantum principle $w=n h$ holds with integer $n \geqq 1$. This, in turn, means that the  metric differential $\partial\left(\bar{\lambda}_{(\kappa \lambda)}^{(\mu v)}\left[\begin{array}{c}\frac{\widehat{\mu v}}{\kappa \lambda} \\ \underline{\kappa \lambda}\end{array}\right]_{+}\right) \sim \mu_{+}{ }^{2} \bar{P}_{(\kappa \lambda)}^{(\mu v)} \quad \operatorname{dem}$—the tensorial  change selector of the protosimplex charge ${ }^{2} \stackrel{(p}{P}_{(\kappa \lambda)}^{(\mu v)}$— must be proportional, where the proportionality factor $\mu_{+}$ is multiplicatively contained in equation (97) because $\bar{\lambda}_{(\kappa \lambda)}^{(\mu \nu)}$ the group $( \pm p)_{(\kappa \lambda)}^{(\mu \nu)}$ directly expresses the inertial resistance. According to the interpretation of  $\bar{\lambda}_{(\kappa \lambda)}^{(\mu v)}\left[\begin{array}{c}\widehat{\mu v} \\ {\left[z \lambda^{+}\right]}\end{array}\right]_{+}$as a selector of a spacetime action density and the step-like nature of metronic condensations in $R_{6}$, the protosimplex charge can, however, only ever change by a constant value. On the other hand, however, ${ }^{2} \bar{g}_{(\mu v)} \neq{ }^{2} \bar{g}_{(\mu v)}^{*}$ and also ${ }^{2} \bar{\gamma}_{(\mu v)} \neq{ }^{2} \stackrel{-}{\gamma}_{(\mu v)}^{*}$, so that the Hermitian $\left[\begin{array}{c}\widehat{\mu v} \\ -\frac{1}{\kappa \lambda}\end{array}\right]_{+}$ are defined in their components over the complex algebraic number field,  which in turn goes back to the complex $R_{6}$ signature ( + + + ---) . This also means, however, that the tensorial change selector

<!--okf:source lang="de"-->
> **Original**
>
> metronische Konstanten sein können. Es muß daher 4 mögliche  Volumenelemente $\partial V_{s}=\beta_{s} \prod_{k=1}^{3-s} \partial_{k}()_{k}$ mit $\beta_{s}=$ const geben, was  auf die Natur der reellen metronischen $R_{3}$-Zellen zurückgeht. Ein setzen dieses Sachverhaltes liefert also $\partial_{V}{ }^{2} \bar{M}_{(\kappa i)}^{(\mu v)} \sim \bar{\lambda}_{(\kappa \lambda)}^{(\mu v)}\left[\begin{array}{c}\widehat{\mu v} \\ {\left[\begin{array}{c}-\alpha \\ +\end{array}\right]_{+}} \\ \text {+ }\end{array}\right.$ und  hierin ist die rechte Seite ein Selektor, der nach den synmetroni schen Untersuchungen aus VI., 3. den Kondensationszustand und  somit nach VII., 1. und $\bar{\lambda}_{(\kappa \lambda)}^{(\mu v)} \perp \vec{Y}$ den Trägheitszustand einer Kopp lungsgruppe $( \pm p)_{(\kappa \lambda)}^{(\mu v)}$ beschreibt. Dieser Sachverhalt wird insbeson dere dadurch transparent, daß dieser Zustand zugleich als Energie dichte interpretiert eine Wirkungsdichte in der $R_{4}$-Projektion des $R_{6}$  beschreibt, und für alle Wirkungen $w$ das Quantenprinzip $w=n h$  mit ganzzahligen $n \geqq 1$ gilt. Dies bedeutet aber wiederum, daß das  Metrondifferential $\partial\left(\bar{\lambda}_{(\kappa \lambda)}^{(\mu v)}\left[\begin{array}{c}\frac{\widehat{\mu v}}{\kappa \lambda} \\ \underline{\kappa \lambda}\end{array}\right]_{+}\right) \sim \mu_{+}{ }^{2} \bar{P}_{(\kappa \lambda)}^{(\mu v)} \quad \operatorname{dem}$ tensoriellen  Änderungsselektor der Protosimplexladung ${ }^{2} \stackrel{(p}{P}_{(\kappa \lambda)}^{(\mu v)}$ proportional sein  muß, worin der Proportionalitätsfaktor $\mu_{+}$der Gleichung (97) mul tiplikativ enthalten ist, weil $\bar{\lambda}_{(\kappa \lambda)}^{(\mu \nu)}$ der Gruppe $( \pm p)_{(\kappa \lambda)}^{(\mu \nu)}$ den Träg heitswiderstand unmittelbar ausdrückt. Nach der Interpretation von  $\bar{\lambda}_{(\kappa \lambda)}^{(\mu v)}\left[\begin{array}{c}\widehat{\mu v} \\ {\left[z \lambda^{+}\right]}\end{array}\right]_{+}$als Selektor einer raumzeitlichen Wirkungsdichte und  des Stufencharakters metronischer Kondensationen im $R_{6}$ kann  sich aber die Protosimplexladung immer nur um einen konstanten  Wert ändern. Andererseits ist aber ${ }^{2} \bar{g}_{(\mu v)} \neq{ }^{2} \bar{g}_{(\mu v)}^{*}$ und auch  ${ }^{2} \bar{\gamma}_{(\mu v)} \neq{ }^{2} \stackrel{-}{\gamma}_{(\mu v)}^{*}$, so daß die hermiteschen $\left[\begin{array}{c}\widehat{\mu v} \\ -\frac{1}{\kappa \lambda}\end{array}\right]_{+}$in ihren Komponen ten über dem komplexen algebraischen Zahlenkörper definiert sind,  was wiederum auf die komplexe $R_{6}$-Signatur ( + + + ---) zurück geht. Dies bedeutet aber auch, daß der tensorielle Änderungsselektor
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 265.

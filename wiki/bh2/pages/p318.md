---
title: "Elementary Structures of Matter, Volume 2 — page 318"
type: page
tags: [bh2, page]
sources: [bh2]
page: 318
lang: EN-US
lang_source: de
---
Limes, where $\bar{\varphi}$ is composed, on the one hand, of the two components of [12] according to $\bar{\varphi}=\bar{\varphi}^{(1)}+\bar{\varphi}^{(2)}$, but $\bar{\varphi}$ can also be related to (+2) and (+(17)) of the triad (+ (127)) in the form of the metronic vector field functions $\bar{\phi}(+2)$ and $\bar{\psi}(+(17))$, because the $R_{3}$ projection of ( + 1) merges into the stratonical near-field ( +7 ). Here, however, it must be taken into account that $\bar{\phi}+\bar{\psi} \neq \bar{\varphi}$ cannot be directly compared with $\bar{\varphi}$. Since the analysis involves a rotationally symmetric boundary surface, $\vec{v} \neq \overrightarrow{0}$ is stationary with $\vec{v}=$ const and $|\vec{v}|=$ const as a rotational motion, such that the axis of rotation coincides with the axis of symmetry; then $\bar{\phi}+\bar{\psi}$ becomes identical to the change in $\bar{\varphi}$ with the elevation angle $\eta$ and the longitude $\Lambda$ on the rotationally symmetric surface. Therefore, either $\partial_{\eta} \partial_{\Lambda} \bar{\varphi}=\bar{\phi}+\bar{\psi}$ or $\bar{\varphi}=S S(\bar{\phi}+\bar{\psi}){ }^{\prime} \partial \eta \partial \Lambda$ can be set. However, $\bar{\phi}+\bar{\psi}=$ is constant with respect to $\eta$ and $A$, so $\bar{\varphi}=(\bar{\phi}+\bar{\psi}) S S$  $\eta$  $A$.  However, in the infinitesimal sense, the intervals $0 \leqq \eta \leqq \pi$ and $0 \leqq \Lambda \leqq 2 \pi$ hold, so that the limit $\lim _{\tau \rightarrow 0} S S$ ð $\eta \partial \Lambda=\int_{0}^{\pi} \int_{0}^{2 \pi} d \eta d \Lambda=2 \pi^{2}=a$ can be formed for the factor in $\bar{\varphi} \rightarrow \bar{\phi}+\bar{\psi}$. This means, however, that the vector fields in $\lim _{\tau \rightarrow 0} \bar{\varphi}$ at the limit values $\tau \rightarrow 0$ are in the $R_{3}$ (4th validity region) are of the form $\lim _{\tau \rightarrow 0} \Phi=\vec{C}$ and $\lim _{\tau \rightarrow 0} \bar{\psi}=\vec{g}$—such components of the $b$ and $a$ hermetics that cannot transfer energy. Thus, $\lim _{\tau \rightarrow 0} \bar{\varphi}=(\vec{C}+\vec{g}) a$ implies $a \operatorname{rot}(\vec{C}+\vec{g})=$  $=\vec{x} \times \vec{y}$, where $\vec{C} \sim \vec{H}$ is the magnetic $b$component and $\vec{g} \sim \vec{\mu}$ is proportional to the orthogonal trajectory field of the gravitational-dynamic  relation $\operatorname{rot} \vec{\mu} \sim \alpha \vec{G}+\sigma \vec{v}$ according to (*). If one assumes that $\vec{\mu}$ and $\vec{H}$ are not related in any way, then in addition to $\operatorname{rot} \vec{g} \sim \alpha \vec{G}+\sigma \vec{v}$ , $\operatorname{rot} \vec{C} \sim \varepsilon_{0} \vec{E}+\varrho \vec{v}$ would also be valid according to electrodynamics if the mass density $\sigma$ (moving with $\vec{v}$) is assigned an electric charge density $\varrho$ (as the source of the electric component $\vec{E}$ of $b$-hermetics). If we now consider the pre-

<!--okf:source lang="de"-->
> **Original**
>
> Limes geschrieben werden, wobei $\bar{\varphi}$ einerseits aus den beiden Kompo nenten von [12] gemäß $\bar{\varphi}=\bar{\varphi}^{(1)}+\bar{\varphi}^{(2)}$ zusammengesetzt ist, doch  kann $\bar{\varphi}$ auch auf (+2) und (+(17)) der Triade (+ (127)) in Form  der metronischen Vektorfeldfunktionen $\bar{\phi}(+2)$ und $\bar{\psi}(+(17))$ bezo gen werden, weil die $R_{3}$-Projektion des ( + 1) in das stratonische Nah wirkungsfeld ( +7 ) übergeht. Hier muß allerdings berücksichtigt wer den, daß $\bar{\phi}+\bar{\psi} \neq \bar{\varphi}$ nicht direkt mit $\bar{\varphi}$ verglichen werden kann. Han delt es sich bei der Betrachtung um eine rotationssymmetrische be grenzende Fläche und erfolgt $\vec{v} \neq \overrightarrow{0}$ stationär mit $\vec{v}=$ const und  $|\vec{v}|=$ const als Rotationsbewegung, derart, daß die Drehachse mit der  Symmetrieachse zusammenfällt, dann wird $\bar{\phi}+\bar{\psi}$ identisch mit' der  Änderung von $\bar{\varphi}$ mit dem Höhenwinkel $\eta$ und dem Längengrad $\Lambda$  auf der rotationssymmetrischen Oberfläche. Es kann also  $\partial_{\eta} \partial_{\Lambda} \bar{\varphi}=\bar{\phi}+\bar{\psi}$ oder $\bar{\varphi}=S S(\bar{\phi}+\bar{\psi}){ }^{\prime} \partial \eta \partial \Lambda$ gesetzt werden. Hierin ist  aber $\bar{\phi}+\bar{\psi}=$ const hinsichtlich $\eta$ und $A$, also $\bar{\varphi}=(\bar{\phi}+\bar{\psi}) S S$ ð $\eta$ ð $A$.  Infinitesimal gelten aber die Intervalle $0 \leqq \eta \leqq \pi$ und $0 \leqq \Lambda \leqq 2 \pi$, so  daß der Limes $\lim _{\tau \rightarrow 0} S S$ ð $\eta \partial \Lambda=\int_{0}^{\pi} \int_{0}^{2 \pi} d \eta d \Lambda=2 \pi^{2}=a$ für den Fak tor in $\bar{\varphi} \rightarrow \bar{\phi}+\bar{\psi}$ gebildet werden kann. Dies bedeutet aber, daß die bei den Grenzwerte $\tau \rightarrow 0$ in $\lim _{\tau \rightarrow 0} \bar{\varphi}$ Vektorfelder im $R_{3}$ (4. Gültig keitsbereich) sind, die in der Form $\lim _{\tau \rightarrow 0} \Phi=\vec{C}$ und $\lim _{\tau \rightarrow 0} \bar{\psi}=\vec{g}$ sol che Komponenten der $b$ - und $a$-Hermetrie sind, die keine Energie  übertragen können. Mit $\lim _{\tau \rightarrow 0} \bar{\varphi}=(\vec{C}+\vec{g}) a$ wird also $a \operatorname{rot}(\vec{C}+\vec{g})=$  $=\vec{x} \times \vec{y}$, worin $\vec{C} \sim \vec{H}$ der magnetischen $b$-Komponente und  $\vec{g} \sim \vec{\mu}$ dem Orthogonaltrajektorienfeld der gravitationsdynamischen  Beziehung $\operatorname{rot} \vec{\mu} \sim \alpha \vec{G}+\sigma \vec{v}$ nach (*) proportional ist. Nimmt man  an, daß $\vec{\mu}$ und $\vec{H}$ nicht in irgendwelchen Zusammenhängen stehen,  dann wäre neben $\operatorname{rot} \vec{g} \sim \alpha \vec{G}+\sigma \vec{v}$ auch $\operatorname{rot} \vec{C} \sim \varepsilon_{0} \vec{E}+\varrho \vec{v}$  nach der Elektrodynamik gültig, wenn der Massendichte $\sigma$ (mit $\vec{v}$  bewegt) eine elektrische Ladungsdichte $\varrho$ zugeordnet wird (als Quelle  der elektrischen Komponente $\vec{E}$ der $b$-Hermetrie). Wird nun die vor-
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 318.

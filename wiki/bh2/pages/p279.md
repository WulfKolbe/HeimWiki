---
title: "Elementary Structures of Matter, Volume 2 — page 279"
type: page
tags: [bh2, page]
sources: [bh2]
page: 279
lang: EN-US
lang_source: de
---
because only for $q \geqq 1$ can $k$ appear in $\eta_{q k}$ according to (98), whereby the terms “external” or “internal” refer to the $R_{3}$ volume of the structure. Similar to the derivation of the charge field components (28a), these potentials can only be that of the reduced, externally acting charge field component, i.e., $V_{R R}$ and that of the internally acting component $V_{\varrho \varrho}(k)$. Consequently, the following relationship holds: 
$V_{1}=E(N, q): E_{k}(k, N, q)=V_{R R}: V_{\varrho \varrho}(k)=\eta_{q} \eta_{q k}^{-1}$. An analogous ratio $V_{2}$ could be defined by $V_{2}=V_{W W}: \underline{V}_{R R}$ if, despite $q \geqq 1$, the potential $\underline{V}_{R R} \sim q^{2} e_{R}^{2}(q=1) f(r)$  were chosen as the reference, along with $e_{R}(q=1)=\varepsilon_{ \pm} \sqrt{\eta_{1}}$, $\eta_{1}=\eta$, and $V_{\varepsilon \varepsilon} \sim q^{2} \varepsilon_{ \pm}^{2} f(r)$. This would also define the quadratic ratios 
$Q_{1}=\left(V_{R R}: \underline{V}_{R \varepsilon}\right)\left(V_{R R}: V_{\varepsilon \varepsilon}\right)$ and $Q_{2}=\left(\underline{V}_{R R}: V_{\varepsilon \varepsilon}\right)\left(V_{R R}: \underline{V}_{R \varepsilon}\right)$. Since in every case $f q^{2}$ and the proportionality factor cancel out from the $V_{x y}$ in these ratios, using (28a) and (98), in addition to $\eta_{q k} V_{1}=\eta_{q}$, $4 \eta V_{2}=\left(1+\sqrt{\eta_{q}}\right)^{2}$, $Q_{1} \sqrt{\eta}=\eta_{q}^{2}$, and $Q_{2}=\sqrt{\eta}$ are also derived.  In fact, the only observable part of the $d$ structure is $(+7)_{d}$ in the $R_{3}$ with the property $q e_{ \pm}$, which is described as the internal structure of $(+7)_{d}$ via $( \pm(35))_{d}$ and $(-6)_{d}$ and determines the partial structure $C \stackrel{\wedge}{=}( \pm 5)-(2)-(-6)-(2)-( \pm 3)-(1)-(-6)-(3)-( \pm 5)$ of the charge field cycle. Here, the $-(j)-$ are connected to $1 \leqq j \leqq 3$  contact conjuncts via $(+(127))_{d}$. This $C$ structure is symbolized by  the signature $\left[\begin{array}{l}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}$, such that $\operatorname{da} \beta\left[\begin{array}{l}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}=\sum_{g}\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\mu_{\nu}\end{array}\right]_{+}$  is expressed as a sum of those fundamental capacitors whose capacitor fluxes and shield fields determine $C$. The  indexing $g$ here merely denotes the capacitor signatures to be summed. If ${ }^{2} \bar{Q}_{(\gamma \delta)}^{(\alpha \beta)}$ is the coupling tensor corresponding to $C$, then in $\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}=\left(1+s p^{2} \bar{Q}_{(\gamma \delta)}^{(\alpha \beta)} \times()\right) ;\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} \quad$ the relation (58) with (58a) in the form $\bar{\lambda}_{(\gamma \delta)}^{(\alpha \beta)} \times\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}-D_{(\gamma \delta)}^{(\alpha \beta)} ;\left[\begin{array}{l}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}=$

<!--okf:source lang="de"-->
> **Original**
>
> denn nur für $q \geqq 1$ kann $k$ in $\eta_{q k}$ nach (98) erscheinen, wobei sich  die Begriffe «extern» oder «intern» auf das $R_{3}$-Volumen der Struktur  beziehen. Ähnlich wie bei der Entwicklung der Ladungsfeldkomponen ten (28a) kann es sich bei diesen Potentialen nur um dasjenige der redu zierten, extern wirkenden Ladungsfeldkomponente, also $V_{R R}$ und das jenige der intern wirkenden Komponente $V_{\varrho \varrho}(k)$ handeln. Mithin gilt  für das Verhältnis 
> $V_{1}=E(N, q): E_{k}(k, N, q)=V_{R R}: V_{\varrho \varrho}(k)=\eta_{q} \eta_{q k}^{-1}$. Ein analoges Ver hältnis $V_{2}$ könnte durch $V_{2}=V_{W W}: \underline{V}_{R R}$ definiert werden, wenn als  Bezug trotz $q \geqq 1$ nach (28a) das Potential $\underline{V}_{R R} \sim q^{2} e_{R}^{2}(q=1) f(r)$  mit $e_{R}(q=1)=\varepsilon_{ \pm} \sqrt{\eta_{1}}$ und $\eta_{1}=\eta$ sowie $V_{\varepsilon \varepsilon} \sim q^{2} \varepsilon_{ \pm}^{2} f(r)$ gewählt  wird. Damit würden auch die quadratischen Verhältnisse 
> $Q_{1}=\left(V_{R R}: \underline{V}_{R \varepsilon}\right)\left(V_{R R}: V_{\varepsilon \varepsilon}\right)$ und $Q_{2}=\left(\underline{V}_{R R}: V_{\varepsilon \varepsilon}\right)\left(V_{R R}: \underline{V}_{R \varepsilon}\right)$ definiert  werden. Da sich in jedem Fall $f q^{2}$ und der Proportionalitätsfaktor aus  den $V_{x y}$ in diesen Verhältnissen kürzt, wird mit (28a) und (98) neben  $\eta_{q k} V_{1}=\eta_{q}$ auch $4 \eta V_{2}=\left(1+\sqrt{\eta_{q}}\right)^{2}$ sowie $Q_{1} \sqrt{\eta}=\eta_{q}^{2}$ und $Q_{2}=\sqrt{\eta}$.  Tatsächlich observabel ist von der $d$-Struktur nur $(+7)_{d}$ im $R_{3}$ mit  der Eigenschaft $q e_{ \pm}$, welche als Internstruktur des $(+7)_{d}$ durch  $( \pm(35))_{d}$ und $(-6)_{d}$ beschrieben wird und die Partialstruktur  $C \stackrel{\wedge}{=}( \pm 5)-(2)-(-6)-(2)-( \pm 3)-(1)-(-6)-(3)-( \pm 5)$  des Ladungsfeldzyklus bedingt. Hier sind die $-(j)-$ mit $1 \leqq j \leqq 3$  Kontaktkonjunktive über $(+(127))_{d}$. Diese $C$-Struktur werde durch  die Signatur $\left[\begin{array}{l}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}$symbolisiert, derart, $\operatorname{da} \beta\left[\begin{array}{l}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}=\sum_{g}\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\mu_{\nu}\end{array}\right]_{+}$  durch eine Summe derjenigen Fundamentalkondensoren ausgedrückt  wird, deren Kondensorflüsse und Schirmfelder $C$ bestimmen. Die  Indizierung $g$ bezeichnet hier lediglich die zur Summation kommen den Kondensorsignaturen. Ist ${ }^{2} \bar{Q}_{(\gamma \delta)}^{(\alpha \beta)}$ der $C$ entsprechende Kopplungs tensor, dann kann auf $\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}=\left(1+s p^{2} \bar{Q}_{(\gamma \delta)}^{(\alpha \beta)} \times()\right) ;\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+} \quad$ die Be ziehung (58) mit (58a) in der Form $\bar{\lambda}_{(\gamma \delta)}^{(\alpha \beta)} \times\left[\begin{array}{c}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}-D_{(\gamma \delta)}^{(\alpha \beta)} ;\left[\begin{array}{l}\widehat{\alpha \beta} \\ \gamma \delta\end{array}\right]_{+}=$
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 279.

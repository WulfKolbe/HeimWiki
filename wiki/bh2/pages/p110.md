---
title: "Elementary Structures of Matter, Volume 2 — page 110"
type: page
tags: [bh2, page]
sources: [bh2]
page: 110
lang: EN-US
lang_source: de
---
the partial structures of the compositional condensation stages $\bar{\lambda}$.  Substituting this decomposition into the matrix spectrum thus yields  $\left.\sum_{\varepsilon} s p \Lambda_{(\mu v)}^{(\kappa \lambda)} ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -{ }_{\mu \nu}\end{array}\right]=\bar{\lambda} \widehat{[ }\right]=\sum_{\varepsilon} \bar{\lambda}_{(\mu \nu)}^{(\kappa \lambda)}\left(1+s p^{2} \bar{Q}_{(\mu \nu)}^{(\kappa \lambda)} \times()\right) ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -{ }_{\mu \nu}\end{array}\right]$. 
The resulting condition  $\sum_{\varepsilon}\left(s p \Lambda_{(\mu v)}^{(\kappa \lambda)} \wedge \bar{\lambda}_{(\mu v)}^{(\kappa \lambda)} \cdot\left(1+s p^{2} \bar{Q}_{(\mu v)}^{(\kappa \lambda)} \times()\right)\right) ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\mu_{\nu}\end{array}\right]={ }^{2} \overline{0} \quad$ can obviously only be satisfied term by term, because the summands are independent. However, this means

<!--okf:source lang="de"-->
> **Original**
>
> den Partialstrukturen der kompositiven Kondensationsstufen $\bar{\lambda}$ sind.  Einsetzen dieser Spaltung in das Matrizenspektrum liefert also  $\left.\sum_{\varepsilon} s p \Lambda_{(\mu v)}^{(\kappa \lambda)} ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -{ }_{\mu \nu}\end{array}\right]=\bar{\lambda} \widehat{[ }\right]=\sum_{\varepsilon} \bar{\lambda}_{(\mu \nu)}^{(\kappa \lambda)}\left(1+s p^{2} \bar{Q}_{(\mu \nu)}^{(\kappa \lambda)} \times()\right) ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -{ }_{\mu \nu}\end{array}\right]$. 
> Die sich hieraus ergebende Bedingung  $\sum_{\varepsilon}\left(s p \Lambda_{(\mu v)}^{(\kappa \lambda)} \wedge \bar{\lambda}_{(\mu v)}^{(\kappa \lambda)} \cdot\left(1+s p^{2} \bar{Q}_{(\mu v)}^{(\kappa \lambda)} \times()\right)\right) ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\mu_{\nu}\end{array}\right]={ }^{2} \overline{0} \quad$ kann offen sichtlich nur gliedweise erfüllt werden, weil eine Unabhängigkeit  der Summanden besteht. Dies bedeutet aber
<!--/okf:source-->

Here, too, no anti-Hermitian tensor selector can exist, so that, using the synmetronic world selector  $L_{(\mu \nu)}^{(\kappa \lambda)}=\Lambda_{(\mu \nu)}^{(\kappa \lambda)}-\bar{\lambda}_{(\mu \nu)}^{(\kappa \lambda)} \times\left(1+s p^{2} \bar{Q}_{(\mu \nu)}^{(\kappa \lambda)} \times()\right)$ after inverting the  matrix spectrum, the synmetronic world selector equation

<!--okf:source lang="de"-->
> **Original**
>
> Auch hier kann kein tensorieller Selektor antihermitescher Art existie ren, so daß sich mit dem synmetronischen Weltselektor  $L_{(\mu \nu)}^{(\kappa \lambda)}=\Lambda_{(\mu \nu)}^{(\kappa \lambda)}-\bar{\lambda}_{(\mu \nu)}^{(\kappa \lambda)} \times\left(1+s p^{2} \bar{Q}_{(\mu \nu)}^{(\kappa \lambda)} \times()\right)$ nach Rückbildung des  Matrizenspektrums die synmetronische Weltselektorgleichung
<!--/okf:source-->

is obtained when the Hermitian property (56a) and (57) are taken into account. 
In this context, the synmetronic world selector is essentially described by the analogue of the space condenser and the qua-drastic correlation selector, which is given by

<!--okf:source lang="de"-->
> **Original**
>
> ergibt, wenn die Hermitezität (56a) und (57) berücksichtigt wird. 
> In dieser Beziehung wird der synmetronische Weltselektor im  wesentlichen durch das Analogon zum Raumkondensor und den qua dratischen Korrelationsselektor beschrieben, was durch
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 110.

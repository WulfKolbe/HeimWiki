---
title: "Elementary Structures of Matter, Volume 2 — page 96"
type: page
tags: [bh2, page]
sources: [bh2]
page: 96
lang: EN-US
lang_source: de
---
${ }^{2} \bar{\gamma},{ }^{2} \bar{\kappa}, \bar{\psi}$ and $\llbracket$ are of primary importance for $\llbracket \rrbracket$; for if  we succeed in specifying this composition explicitly, then the general synmetronic fundamental problem can be formulated via (19).  To carry out this program, it seems sensible to first explicitly investigate the influence of the tensor-analytic differential selectors on the matrix spectra of tensor products, because with the reduction ${ }^{2} \bar{\kappa}_{(\alpha)}={ }^{2} \bar{\alpha}$ all elements of $\hat{\gamma}$ appear in the form ${ }^{2} \bar{\gamma}_{(a b)}=s p\left({ }^{2} \bar{a} \times{ }^{2} \bar{b}\right)$. However, such tensor-analytic investigations are explicitly presented in III., so that $\left(\operatorname{ROT}_{(x)}()\right)_{i k}=\underline{\partial}_{k}()_{i}-\underline{\partial}_{i}()_{k}$ and $\left(\widehat{\operatorname{DIV}}_{(x)}()\right)_{i k}=\underline{\partial}_{i}()_{k}$ the covariant condenser $[i k l]_{(a b)}$ can be transformed if $(x)$ the reference to $x_{k}=C_{k} ; n$ is indexed by $1 \leqq k \leqq 6$ of $R_{6(0)}$. This yields, in the  limit $\lim _{\tau \rightarrow 0}[\widehat{a b}] ; n=\widehat{\{ \}}_{(a b)}$, the infinitesimal representation

<!--okf:source lang="de"-->
> **Original**
>
> ${ }^{2} \bar{\gamma},{ }^{2} \bar{\kappa}, \bar{\psi}$ und $\llbracket$ ist dasjenige für $\llbracket \rrbracket$ von primärer Bedeutung; denn wenn  es gelingt, diese Komposition explizit anzugeben, dann kann über (19)  das allgemeine synmetronische Fundamentalproblem gestellt werden.  Zur Durchführung dieses Programmes scheint es sinnvoll, zunächst  den Einfluß der tensoranalytischen Differentialselektoren auf die  Matrizenspektren von Tensorprodukten explizit zu untersuchen, weil  mit der Kürzung ${ }^{2} \bar{\kappa}_{(\alpha)}={ }^{2} \bar{\alpha}$ alle Elemente von $\hat{\gamma}$ in der Form  ${ }^{2} \bar{\gamma}_{(a b)}=s p\left({ }^{2} \bar{a} \times{ }^{2} \bar{b}\right)$ erscheinen. Derartige tensoranalytische Unter suchungen liegen jedoch explizit in III. vor, so daß mit  $\left(\operatorname{ROT}_{(x)}()\right)_{i k}=\underline{\partial}_{k}()_{i}-\underline{\partial}_{i}()_{k}$ und $\left(\widehat{\operatorname{DIV}}_{(x)}()\right)_{i k}=\underline{\partial}_{i}()_{k}$ der kovariante  Kondensor $[i k l]_{(a b)}$ umgeformt werden kann, wenn $(x)$ die Bezugnahme  auf $x_{k}=C_{k} ; n$ mit $1 \leqq k \leqq 6$ des $R_{6(0)}$ indiziert. Es ergibt sich im  Limes $\lim _{\tau \rightarrow 0}[\widehat{a b}] ; n=\widehat{\{ \}}_{(a b)}$ die infinitesimale Darstellung
<!--/okf:source-->

According to the laws of infinitesimal differentiation, the quadratic derivatives converge to zero as the limit approaches zero.

<!--okf:source lang="de"-->
> **Original**
>
> Gesetzen der infinitesimalen Differentiation die quadratischen Metron differentiale im Limes gegen Null konvergieren.
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 96.

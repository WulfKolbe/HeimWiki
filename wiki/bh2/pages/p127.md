---
title: "Elementary Structures of Matter, Volume 2 — page 127"
type: page
tags: [bh2, page]
sources: [bh2]
page: 127
lang: EN-US
lang_source: de
---
However, this system does not yet provide any indication of the explicit evaluation of the Metron integral. If $(\kappa \lambda) \neq(\alpha \beta)$ are two different counter-signatures to the same base signature $(\mu \nu)$, then, due to the identity in the comparison $\boldsymbol{S} q_{(\mu \nu) k l}^{(\kappa \lambda)} \psi_{(\mu \nu) k l}^{(\kappa \lambda)} \partial \mu=\boldsymbol{S} q_{(\mu \nu) k l}^{(\alpha \beta)} \psi_{(\mu \nu) k l}^{(\alpha \beta)}{ }^{\delta} \mu$ or the transformation law for foreign-field correlators with the same basis signature,

<!--okf:source lang="de"-->
> **Original**
>
> doch gibt dieses System noch keinen Hinweis auf die explizite Ausfüh rung des Metronintegrals. Sind $(\kappa \lambda) \neq(\alpha \beta)$ zwei verschiedene Kon trasignaturen zur gleichen Basissignatur $(\mu \nu)$, dann folgt wegen der  Identität im Vergleich $\boldsymbol{S} q_{(\mu \nu) k l}^{(\kappa \lambda)} \psi_{(\mu \nu) k l}^{(\kappa \lambda)} \partial \mu=\boldsymbol{S} q_{(\mu \nu) k l}^{(\alpha \beta)} \psi_{(\mu \nu) k l}^{(\alpha \beta)}{ }^{\delta} \mu$ oder  das Umformungsgesetz der Fremdfeldkorrelatoren bei gleicher Basis signatur
<!--/okf:source-->

after the formation of the Metrondifferential. Clearly, however, this relationship must also hold for $(\alpha \beta)=(\mu \nu)$, while $(\kappa \lambda) \neq(\mu \nu)$ remains unchanged. Finally, if we assume the fundamental condenser $\left[\begin{array}{c}\widehat{v \mu} \\ -\frac{1}{\mu \nu}\end{array}\right]_{+}=\left[\begin{array}{c}\widehat{\mu \nu} \\ +\overline{\mu \nu}\end{array}\right]_{+}$, then there can only be a pseudo-correlation, so that the correlation tensor becomes ${ }^{2} \bar{Q}_{(\mu \nu)}^{(\nu \mu)}={ }^{2} \bar{E}$. In the substitution $q_{(\mu \nu)}^{(\kappa \lambda)}=q_{(\mu \nu) k l}^{(\nu \mu)} \psi_{(\mu \nu) k l}^{(\nu \mu)}\left(\psi_{(\mu \nu) k l}^{(\kappa \lambda)}\right)^{-1}$, the following therefore holds for the far-field correlator

<!--okf:source lang="de"-->
> **Original**
>
> nach Bildung des Metrondifferentials. Ganz eindeutig muß diese  Beziehung aber auch für $(\alpha \beta)=(\mu \nu)$ gelten, während $(\kappa \lambda) \neq(\mu \nu)$  bleibt. Wird schließlich noch der Fundamentalkondensor  $\left[\begin{array}{c}\widehat{v \mu} \\ -\frac{1}{\mu \nu}\end{array}\right]_{+}=\left[\begin{array}{c}\widehat{\mu \nu} \\ +\overline{\mu \nu}\end{array}\right]_{+}$angenommen, dann kann es nur eine Pseudo korrelation geben, so daß der Korrelationstensor ${ }^{2} \bar{Q}_{(\mu \nu)}^{(\nu \mu)}={ }^{2} \bar{E}$  wird. In der Substitution $q_{(\mu \nu)}^{(\kappa \lambda)}=q_{(\mu \nu) k l}^{(\nu \mu)} \psi_{(\mu \nu) k l}^{(\nu \mu)}\left(\psi_{(\mu \nu) k l}^{(\kappa \lambda)}\right)^{-1}$ gilt also  für den Fremdfeldkorrelator
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 127.

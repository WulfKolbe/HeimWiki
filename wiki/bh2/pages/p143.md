---
title: "Elementary Structures of Matter, Volume 2 — page 143"
type: page
tags: [bh2, page]
sources: [bh2]
page: 143
lang: EN-US
lang_source: de
---
must be supplemented by the one that holds in the micromare range. In this approximation range, the covariant coupling extrema thus form integer-valued discrete eigenvalues, whereas the contravariant coupling extrema coincide with those of the covariant spectrum,  in which the extrema of the lattice kernel determinant lie. 

In the case of time and space condensations, the correlator is acted upon by the same class of sieve operators, so that $\hat{\gamma}_{(b)}$ and $\hat{\gamma}_{(c)}$ are formally related with respect to the elements to the extent that the introductory investigation of the possible condensers for the two cases ( $b$ ) and $(c)$ can be carried out uniformly. If the abbreviations ${ }^{2} \bar{\gamma}_{(\kappa \lambda)} \equiv \kappa, \lambda$, ${ }^{2} \bar{\kappa}_{(\mu)} \equiv \mu$, and $\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\frac{1}{\mu \nu}\end{array}\right]_{+} \equiv\left[\begin{array}{c}\kappa \lambda \\ \mu \nu\end{array}\right]$ are introduced,  then for both the time and space condensations, there are 30 condensers each resulting from $\hat{\gamma}_{(b)}$ and $\hat{\gamma}_{(c)}$. Formally, the following scheme applies to these condensers: $\left[\begin{array}{ll}1 & 1 \\ 1 & \alpha\end{array}\right],\left[\begin{array}{cc}1 & 1 \\ \alpha\end{array}\right],\left[\begin{array}{ll}1 & 1 \\ \alpha & 1\end{array}\right],\left[\begin{array}{ll}1 & 1 \\ \alpha & \alpha\end{array}\right],\left[\begin{array}{cc}1 & \alpha \\ 1 & 1\end{array}\right],\left[\begin{array}{cc}1 & \alpha \\ 1\end{array}\right]$, $\left[\begin{array}{ll}1 & \alpha \\ \alpha & 1\end{array}\right],\left[\begin{array}{ll}1 & \alpha \\ \alpha & \alpha\end{array}\right],\left[\begin{array}{cc}1 & \alpha \\ \alpha\end{array}\right],\left[\begin{array}{c}1 \\ 1\end{array} \alpha\right],\left[\begin{array}{c}1 \\ \alpha\end{array}\right],\left[\begin{array}{c}1 \\ \alpha\end{array}\right],\left[\begin{array}{l}1 \\ \alpha\end{array}\right],\left[\begin{array}{ll}\alpha & 1 \\ 1 & 1\end{array}\right],\left[\begin{array}{ll}\alpha & 1 \\ 1 & \alpha\end{array}\right],\left[\begin{array}{cc}\alpha & 1 \\ 1\end{array}\right],\left[\begin{array}{ll}\alpha & 1 \\ \alpha & \alpha\end{array}\right]$,  $\left[\begin{array}{cc}\alpha & 1 \\ \alpha\end{array}\right],\left[\begin{array}{ll}\alpha & \alpha \\ 1 & 1\end{array}\right],\left[\begin{array}{ll}\alpha & \alpha \\ 1 & \alpha\end{array}\right],\left[\begin{array}{cc}\alpha & \alpha \\ 1\end{array}\right],\left[\begin{array}{ll}\alpha & \alpha \\ \alpha & 1\end{array}\right],\left[\begin{array}{cc}\alpha & \alpha \\ \alpha\end{array}\right],\left[\begin{array}{c}\alpha \\ 1\end{array}\right],\left[\begin{array}{c}\alpha \\ 1\end{array}\right],\left[\begin{array}{c}\alpha \\ 1\end{array}\right],\left[\begin{array}{c}\alpha \\ \alpha\end{array}\right],\left[\begin{array}{c}\alpha \\ \alpha\end{array}\right]$, and  $\left[\begin{array}{cc}1 & 1 \\ 1\end{array}\right],\left[\begin{array}{cc}1 \\ 1 & 1\end{array}\right]$. Depending on whether $\alpha=2$ or $\alpha=3$ is set, the condensers are formed from $\hat{\gamma}_{(b)}(\alpha=2)$ or $\hat{\gamma}_{(c)}(\alpha=3)$. Typical for cases (b) and (c) is the joint continuation of the condensation in the ( $x_{5}, x_{6}$ ) region via $\left[\begin{array}{cc}1 & 1 \\ 1\end{array}\right]_{+}=\left[\begin{array}{l}\gamma \\ \kappa\end{array}\right]_{+}$ and $\left[\begin{array}{c}1 \\ 1 \\ 1\end{array}\right]_{+}=\left[\begin{array}{l}\kappa \\ \gamma\end{array}\right]_{+}$  in the form of a $a$ complement, for whose self-condensations the coupling equations (69) and (69a) apply. Due to this intrinsic nature, the condensations $b$ and $c$, that is, the spectra of the imponderable photons (time condensations $b$) and the ponderable neutrocorpuscles (space condensations $c$ ), must be physically closely related. This relationship applies only to the structure of the condenser signatures, but not to the linear aggregate $\mu$ of the lattice selectors. In the case $b$, $\mu$ is imaginary, as in $a$, but in $c$

<!--okf:source lang="de"-->
> **Original**
>
> ergänzt werden, die im mikromaren Bereich gilt. In diesem Approxi mationsbereich bilden also die kovarianten Kopplungsextrema grad zahlig diskrete Eigenwerte, wogegen die kontravarianten Kopplungs extrema mit denjenigen des kovarianten Spektrums zusammenfallen,  in denen die Extrema der Gitterkerndeterminante liegen. 
>
> Bei den Zeit- und Raumkondensationen wirkt auf den Korrelator die  gleiche Klasse Sieboperatoren, so daß $\hat{\gamma}_{(b)}$ und $\hat{\gamma}_{(c)}$ hinsichtlich der  Elemente formal soweit verwandt sind, daß die einleitende Untersu chung der möglichen Kondensoren für die beiden Fälle ( $b$ ) und $(c)$  einheitlich durchgeführt werden kann. Werden die Abkürzungen  ${ }^{2} \bar{\gamma}_{(\kappa \lambda)} \equiv \kappa, \lambda$ sowie ${ }^{2} \bar{\kappa}_{(\mu)} \equiv \mu$ und $\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\frac{1}{\mu \nu}\end{array}\right]_{+} \equiv\left[\begin{array}{c}\kappa \lambda \\ \mu \nu\end{array}\right]$ eingeführt,  dann ergeben sich sowohl für die Zeit- als auch für die Raumkondensa tionen jeweils 30 Kondensoren aus $\hat{\gamma}_{(b)}$ und $\hat{\gamma}_{(c)}$. Für diese Konden soren gilt formal das Schema $\left[\begin{array}{ll}1 & 1 \\ 1 & \alpha\end{array}\right],\left[\begin{array}{cc}1 & 1 \\ \alpha\end{array}\right],\left[\begin{array}{ll}1 & 1 \\ \alpha & 1\end{array}\right],\left[\begin{array}{ll}1 & 1 \\ \alpha & \alpha\end{array}\right],\left[\begin{array}{cc}1 & \alpha \\ 1 & 1\end{array}\right],\left[\begin{array}{cc}1 & \alpha \\ 1\end{array}\right]$,  $\left[\begin{array}{ll}1 & \alpha \\ \alpha & 1\end{array}\right],\left[\begin{array}{ll}1 & \alpha \\ \alpha & \alpha\end{array}\right],\left[\begin{array}{cc}1 & \alpha \\ \alpha\end{array}\right],\left[\begin{array}{c}1 \\ 1\end{array} \alpha\right],\left[\begin{array}{c}1 \\ \alpha\end{array}\right],\left[\begin{array}{c}1 \\ \alpha\end{array}\right],\left[\begin{array}{l}1 \\ \alpha\end{array}\right],\left[\begin{array}{ll}\alpha & 1 \\ 1 & 1\end{array}\right],\left[\begin{array}{ll}\alpha & 1 \\ 1 & \alpha\end{array}\right],\left[\begin{array}{cc}\alpha & 1 \\ 1\end{array}\right],\left[\begin{array}{ll}\alpha & 1 \\ \alpha & \alpha\end{array}\right]$,  $\left[\begin{array}{cc}\alpha & 1 \\ \alpha\end{array}\right],\left[\begin{array}{ll}\alpha & \alpha \\ 1 & 1\end{array}\right],\left[\begin{array}{ll}\alpha & \alpha \\ 1 & \alpha\end{array}\right],\left[\begin{array}{cc}\alpha & \alpha \\ 1\end{array}\right],\left[\begin{array}{ll}\alpha & \alpha \\ \alpha & 1\end{array}\right],\left[\begin{array}{cc}\alpha & \alpha \\ \alpha\end{array}\right],\left[\begin{array}{c}\alpha \\ 1\end{array}\right],\left[\begin{array}{c}\alpha \\ 1\end{array}\right],\left[\begin{array}{c}\alpha \\ 1\end{array}\right],\left[\begin{array}{c}\alpha \\ \alpha\end{array}\right],\left[\begin{array}{c}\alpha \\ \alpha\end{array}\right]$ und  $\left[\begin{array}{cc}1 & 1 \\ 1\end{array}\right],\left[\begin{array}{cc}1 \\ 1 & 1\end{array}\right]$. Jenachdem, ob $\alpha=2$ oder $\alpha=3$ gesetzt wird, entstehen  die Kondensoren aus $\hat{\gamma}_{(b)}(\alpha=2)$ oder $\hat{\gamma}_{(c)}(\alpha=3)$. Typisch für die  Fälle (b) und (c) ist die gemeinsame Fortsetzung der Kondensation  in den ( $x_{5}, x_{6}$ )-Bereich durch $\left[\begin{array}{cc}1 & 1 \\ 1\end{array}\right]_{+}=\left[\begin{array}{l}\gamma \\ \kappa\end{array}\right]_{+}$und $\left[\begin{array}{c}1 \\ 1 \\ 1\end{array}\right]_{+}=\left[\begin{array}{l}\kappa \\ \gamma\end{array}\right]_{+}$  in Form eines $a$-Komplementes für dessen Selbstkondensationen die  Kopplungsgleichungen (69) und (69a) gelten. Durch diese Eigen tümlichkeit müssen die Kondensationen $b$ und $c$, also die Spektren  der imponderablen Photonen (Zeitkondensationen $b$ ) und der pon derablen Neutrokorpuskeln (Raomkondensationen $c$ ) physikalisch  nahe verwandt sein. Diese Verwandtschaft bezieht sich nur auf die  Struktur der Kondensorsignaturen, nicht aber auf das Linearaggregat $\mu$  der Gitterselektoren. Im Fall $b$ ist $\mu$ wie in $a$ imaginär, aber in $c$
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 143.

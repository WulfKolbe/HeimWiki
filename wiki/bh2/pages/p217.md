---
title: "Elementary Structures of Matter, Volume 2 — page 217"
type: page
tags: [bh2, page]
sources: [bh2]
page: 217
lang: EN-US
lang_source: de
---
${\underset{a}{S}}_{S_{v}} \partial v=\stackrel{I}{\underset{a}{S}(s+1)} \partial v-\overbrace{a}^{I} v ð v=(s+1)(I+1-a)-\frac{I}{2}(I+1)+$ 
$+\frac{a}{2}(a-1)$ is obtained when the methods from Section III, 1 are applied. In the Metron integral, $a=1$ holds because $1 \leqq \nu \leqq I$ applies to the definition interval of the multiplet components. On the other hand, however, since $a=1$ holds due to $-s \leqq s_{\nu} \leqq+s$, $\underset{a=1}{I} s_{\nu} \partial \nu=\sum_{\nu=1}^{I} s_{\nu}=0$ also holds,  which, when substituted with $a=1$, yields $0=(s+1) I-(I+1) I / 2=$ $=I s-(I-1) I / 2$, thus yielding $I=2 s+1=P+1$ with $2 s=P$ for the number of spin-isomorphically transformable $\bar{\lambda}$ terms of such a multiplet. Consequently, the spatial spin isomorphism induced by $s$ from $\sigma_{t}$ is described by

<!--okf:source lang="de"-->
> **Original**
>
> ${\underset{a}{S}}_{S_{v}} \partial v=\stackrel{I}{\underset{a}{S}(s+1)} \partial v-\overbrace{a}^{I} v ð v=(s+1)(I+1-a)-\frac{I}{2}(I+1)+$ 
> $+\frac{a}{2}(a-1)$ liefert, wenn die Methoden aus III., 1. zur Anwendung  kommen. In dem Metronintegral ist $a=1$, weil für das Definitionsin tervall der Multiplettkomponenten $1 \leqq \nu \leqq I$ gilt. Andererseits wird  aber mit $a=1$ wegen $-s \leqq s_{\nu} \leqq+s$ auch $\underset{a=1}{I} s_{\nu} \partial \nu=\sum_{\nu=1}^{I} s_{\nu}=0$,  was mit $a=1$ eingesetzt $0=(s+1) I-(I+1) I / 2=$  $=I s-(I-1) I / 2$, also für die Zahl spinisomorph transformierbarer  $\bar{\lambda}$-Terme eines solchen Multipletts $I=2 s+1=P+1$ mit $2 s=P$  ergibt. Mithin wird also der durch $s$ aus $\sigma_{t}$ bedingte Raumspinisomor phismus beschrieben durch
<!--/okf:source-->

Without a doubt, $s=P=0$ must have the singlet $I=1$  must exist, so that $P \geqq 0$ must hold for all integers, since negative  $s$ values must be excluded by definition. Since, on the other hand, $P<\infty$ must hold, there exists a bound $P \leqq P_{\max }$, which remains finite as a positive integer, so that for $P$ from (90), the closed interval $0 \leqq P \leqq P_{\text {max }}<\infty$ must exist. This upper $P$ bound thus gives the maximum number of spin-isomorphic $\bar{\lambda}$ of the $c$ - and $d$ hermetic parts of a multiplet, whereby the components of such a multiplet can consist equally of $c$ and $d$ terms.  For this reason, $P_{\text {max }}$ should be understood as a characteristic number that defines a structural class of stratonic $R_{3}$ fields ( +7 ) with respect to the $( \pm p)$ fields—singularly mapped in the $R_{3}$ and $p \leqq 6$ in the form of  $P_{\text {max }}+1$ multiplets. On the other hand, however, the configuration number $k$ defined in (72) is also a characteristic number for $(+7)$ , so that $P_{\text {max }}=G(k)$ must be given by the positive integer $G>0$, which in turn depends solely on $k$. However, due to the projection of $( \pm p)$—which defines the structure $(+7)$ according to (79)—onto $R_{3}$ (in the case of the forms $c$ and $d$), $G$, according to (90), does not merely give the

<!--okf:source lang="de"-->
> **Original**
>
> Ganz ohne Zweifel muß es für $s=P=0$ das Singulett $I=1$  geben, so daß für die ganzen Zahlen $P \geqq 0$ gelten muß, weil negative  $s$-Werte von der Definition her auszuschließen sind. Da andererseits  $P<\infty$ bleiben muß, existiert eine Schranke $P \leqq P_{\max }$, die als positive  ganze Zahl endlich bleibt, so daß für $P$ aus (90) das geschlossene  Intervall $0 \leqq P \leqq P_{\text {max }}<\infty$ existieren muß. Diese obere $P$-Schranke  gibt also die maximale Zahl spinisomorpher $\bar{\lambda}$ der $c$ - und $d$ - Hermetrie eines Multipletts an, wobei die Komponenten eines solchen  Multipletts aus $c$ - und $d$-Termen gleichermaßen bestehen können.  Aus diesem Grunde ist $P_{\text {max }}$ als eine Kennziffer zu verstehen, welche  eine strukturelle Klasse stratonischer $R_{3}$-Felder ( +7 ) hinsichtlich der  in den $R_{3}$ singulär abgebildeten $( \pm p)$ mit $p \leqq 6$ in Form von  $P_{\text {max }}+1$ Multipletts umschreibt. Andererseits ist aber die in (72) defi nierte Konfigurationszahl $k$ ebenfalls eine für $(+7)$ charakteristische  Kennziffer, so daß $P_{\text {max }}=G(k)$ durch die positive ganze Zahl $G>0$  gegeben sein muß, die ihrerseits allein von $k$ abhängt. Wegen der Pro jektion der nach (79) die Struktur $(+7)$ definierenden $( \pm p)$ in den  $R_{3}$ (im Fall der Formen $c$ und $d$ ) gibt aber $G$ nach (90) nicht nur die
<!--/okf:source-->

Without a doubt, $s=P=0$ must have the singlet $I=1$  must exist, so that $P \geqq 0$ must hold for all integers, since negative  $s$ values are excluded by definition. Since, on the other hand, $P<\infty$ must hold, there exists a bound $P \leqq P_{\max }$, which remains finite as a positive integer, so that for $P$ from (90), the closed interval $0 \leqq P \leqq P_{\text {max }}<\infty$ must exist. This upper $P$ bound thus gives the maximum number of spin-isomorphic $\bar{\lambda}$ of the $c$ - and $d$ hermetic parts of a multiplet, whereby the components of such a multiplet can consist equally of $c$ and $d$ terms.  For this reason, $P_{\text {max }}$ should be understood as a characteristic number that defines a structural class of stratonic $R_{3}$ fields ( + 7) with respect to the $( \pm p)$ fields—which are singularly mapped in the $R_{3}$—singularly mapped $( \pm p)$ with $p \leqq 6$ in the form of  $P_{\text {max }}+1$ multiplets. On the other hand, however, the configuration number $k$ defined in (72) is also a , so that $P_{\text {max }}=G(k)$ must be given by the positive integer $G>0$, which in turn depends solely on $k$. However, due to the projection of ( $\pm p$ ), which defines the structure ( +7 ) according to (79), onto  $R_{3}$ (in the case of the forms $c$ and $d$), $G$ according to (90) does not merely give the

<!--okf:source lang="de"-->
> **Original**
>
> Ganz ohne Zweifel muß es für $s=P=0$ das Singulett $I=1$  geben, so daß für die ganzen Zahlen $P \geqq 0$ gelten muß, weil negative  $s$-Werte von der Definition her auszuschließen sind. Da andererseits  $P<\infty$ bleiben muß, existiert eine Schranke $P \leqq P_{\max }$, die als positive  ganze Zahl endlich bleibt, so daß für $P$ aus (90) das geschlossene  Intervall $0 \leqq P \leqq P_{\text {max }}<\infty$ existieren muß. Diese obere $P$-Schranke  gibt also die maximale Zahl spinisomorpher $\bar{\lambda}$ der $c$ - und $d$ - Hermetrie eines Multipletts an, wobei die Komponenten eines solchen  Multipletts aus $c$ - und $d$-Termen gleichermaßen bestehen können.  Aus diesem Grunde ist $P_{\text {max }}$ als eine Kennziffer zu verstehen, welche  eine strukturelle Klasse stratonischer $R_{3}$-Felder ( + 7) hinsichtlich der  in den $R_{3}$ singulär abgebildeten $( \pm p)$ mit $p \leqq 6$ in Form von  $P_{\text {max }}+1$ Multipletts umschreibt. Andererseits ist aber die in (72) defi nierte Konfigurationszahl $k$ ebenfalls eine für $(+7)$ charakteristische  Kennziffer, so daß $P_{\text {max }}=G(k)$ durch die positive ganze Zahl $G>0$  gegeben sein muß, die ihrerseits allein von $k$ abhängt. Wegen der Pro jektion der nach (79) die Struktur ( +7 ) definierenden ( $\pm p$ ) in den  $R_{3}$ (im Fall der Formen $c$ und $d$ ) gibt aber $G$ nach (90) nicht nur die
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 217.

---
title: "Elementary Structures of Matter, Volume 2 — page 109"
type: page
tags: [bh2, page]
sources: [bh2]
page: 109
lang: EN-US
lang_source: de
---
Selectors $D_{(\mu v) m}^{(\kappa \lambda)}=\delta(\underline{k}, s) ;() \sum_{\varepsilon}\left[\begin{array}{c}s \\ k m\end{array}\right]_{(\beta \alpha)}^{(\gamma \delta)}(1-\delta)-$

<!--okf:source lang="de"-->
> **Original**
>
> Selektors $D_{(\mu v) m}^{(\kappa \lambda)}=\delta(\underline{k}, s) ;() \sum_{\varepsilon}\left[\begin{array}{c}s \\ k m\end{array}\right]_{(\beta \alpha)}^{(\gamma \delta)}(1-\delta)-$
<!--/okf:source-->

$D_{(\mu \nu) m}^{(\kappa \lambda)} ;\left[\begin{array}{c}i \\ k l\end{array}\right]_{(\mu \nu)}^{(\kappa \lambda)}$ should apply to the $R_{6}$ components. Substituting into $\bar{\lambda} \times[]$ yields, if the functional selector $\Lambda_{(\mu \nu)}^{(\kappa \lambda)}=K_{(\mu \nu)}^{(\kappa \lambda)} ;\left(1+s p^{2} \bar{Q}_{(\mu \nu)}^{(\kappa \lambda)} \times()\right)+D_{(\mu \nu)}^{(\kappa \lambda)}$ is set for the $K$,  $\bar{\lambda} \times \widehat{\prod}=\sum_{\varepsilon} \Lambda_{(\mu v)}^{(\kappa \lambda)} ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\mu v\end{array}\right]$. Formation of the matrix spectrum and the  macroscopic transition $\bar{\lambda}[] \rightarrow \alpha^{2} \bar{W}$ with $\alpha=$ constant, as well as the infinitesimal transition of the action of the energy density selector  $\lim _{\tau \rightarrow 0}{ }^{2} \bar{W} ; n=\overrightarrow{\vec{T}}-\overrightarrow{\vec{g}} \frac{T}{4}=\overrightarrow{\vec{T}}=\underline{\vec{T}}^{\times}$, yields, after $R_{3}$ integration, the energy tensor of the correlation structure $\overrightarrow{\vec{U}}=\int_{V} \overrightarrow{\underline{T}} d V$. 
However, this energy must be composed additively from the energetic states of the partial structures (condenser signatures), so that the energy equation of the correlation structure $\overrightarrow{\vec{U}}=\sum_{\varepsilon} \overrightarrow{\vec{U}}_{(\mu \nu)}^{(\kappa \lambda)} \quad$ holds. This also implies $\underline{\vec{T}}=\sum_{\varepsilon} \vec{I}_{(\mu \nu)}^{(\kappa \lambda)} \rightarrow \sum_{\varepsilon} 2^{2} \bar{W}_{(\mu \nu)}^{(\kappa \lambda)} ; n^{\varepsilon}$ and therefore also mikromar $\alpha \sum_{\varepsilon}{ }^{2} \bar{W}_{(\mu v)}^{(\kappa \lambda)} ; n \leftarrow \sum_{\varepsilon} \bar{\lambda}_{(\mu v)}^{(\kappa \lambda)}\left(1+s p^{2} \bar{Q}_{(\mu v)}^{(\kappa \lambda)} \times()\right) ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -{ }_{\mu v}\end{array}\right] ; n=$  $=\bar{\lambda}[] ; n$, if the $\bar{\lambda}_{(\mu \nu)}^{(\kappa \lambda)}$ corresponding to the condenser signatures—

<!--okf:source lang="de"-->
> **Original**
>
> $D_{(\mu \nu) m}^{(\kappa \lambda)} ;\left[\begin{array}{c}i \\ k l\end{array}\right]_{(\mu \nu)}^{(\kappa \lambda)}$ für die $R_{6}$-Komponenten gelten soll. Einsetzen in  $\bar{\lambda} \times[]$ liefert, wenn für den $K$ entsprechenden Funktionalselektor  $\Lambda_{(\mu \nu)}^{(\kappa \lambda)}=K_{(\mu \nu)}^{(\kappa \lambda)} ;\left(1+s p^{2} \bar{Q}_{(\mu \nu)}^{(\kappa \lambda)} \times()\right)+D_{(\mu \nu)}^{(\kappa \lambda)}$ gesetzt wird,  $\bar{\lambda} \times \widehat{\prod}=\sum_{\varepsilon} \Lambda_{(\mu v)}^{(\kappa \lambda)} ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -\mu v\end{array}\right]$. Bildung des Matrizenspektrums und der  makromare Übergang $\bar{\lambda}[] \rightarrow \alpha^{2} \bar{W}$ mit $\alpha=$ const., sowie dem infini tesimalen Übergang der Wirkung des Energiedichteselektors  $\lim _{\tau \rightarrow 0}{ }^{2} \bar{W} ; n=\overrightarrow{\vec{T}}-\overrightarrow{\vec{g}} \frac{T}{4}=\overrightarrow{\vec{T}}=\underline{\vec{T}}^{\times}$liefert nach $R_{3}$-Integration für  den Energietensor des Korrelationsgefüges $\overrightarrow{\vec{U}}=\int_{V} \overrightarrow{\underline{T}} d V$. 
> Diese Energie muß sich aber additiv aus den energetischen Zuständen  der Partialstrukturen (Kondensorsignaturen) zusammensetzen, so daß  für den Energiesatz des Korrelationsgefüges $\overrightarrow{\vec{U}}=\sum_{\varepsilon} \overrightarrow{\vec{U}}_{(\mu \nu)}^{(\kappa \lambda)} \quad$ gilt. Dies  bedeutet aber auch $\underline{\vec{T}}=\sum_{\varepsilon} \vec{I}_{(\mu \nu)}^{(\kappa \lambda)} \rightarrow \sum_{\varepsilon} 2^{2} \bar{W}_{(\mu \nu)}^{(\kappa \lambda)} ; n^{\varepsilon}$ und daher auch  mikromar $\alpha \sum_{\varepsilon}{ }^{2} \bar{W}_{(\mu v)}^{(\kappa \lambda)} ; n \leftarrow \sum_{\varepsilon} \bar{\lambda}_{(\mu v)}^{(\kappa \lambda)}\left(1+s p^{2} \bar{Q}_{(\mu v)}^{(\kappa \lambda)} \times()\right) ;\left[\begin{array}{c}\widehat{\kappa \lambda} \\ -{ }_{\mu v}\end{array}\right] ; n=$  $=\bar{\lambda}[] ; n$, wenn die $\bar{\lambda}_{(\mu \nu)}^{(\kappa \lambda)}$ die den Kondensorsignaturen entsprechen-
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 109.

---
title: "Elementary Structures of Matter, Volume 2 — page 215"
type: page
tags: [bh2, page]
sources: [bh2]
page: 215
lang: EN-US
lang_source: de
---
which, because of $e^{i \varphi}=\cos \varphi+i \sin \varphi$, leads to $\cos \left(\frac{\pi}{2}\right)=0$ and $\sin \left(\frac{\pi}{2}\right)=1 \mathrm{zu}$  $\exp (i(\pi J+\varphi))=i e^{i \pi J}$, which leads to $z=i \alpha_{p} e^{i \pi J} \quad$. It is $e^{i \pi J}=$  $=\cos (\pi J)+\operatorname{isin}(\pi J)$, while the following applies to $J=Q / 2$. The positive integers $Q \geqq O$ yield integer or half-integer $J$ values.  In the case of $Q=2 n$, $J=n$ becomes an integer, which implies $\cos \pi n= \pm 1$ and  $\sin (\pi n)=0$, i.e., $\cos (\pi J)+i \sin (\pi J)= \pm 1$ under certain conditions. If, on the other hand,  $Q=2 n+1$, that is, if $J$ is a half-integer, then $\cos (\pi J)=0$ and $\sin (\pi J)=1$ hold, that is, $\cos (\pi J)+i \sin (\pi J)=i$. Since there are no other possibilities for $Q$, $e^{i \pi J}=\cos (\pi J)+i \sin (\pi J)=(-1)^{J}$ can be set, which yields $z(J)=i \alpha_{p}(-1)^{J}$. Here, $\alpha_{p}(-1)^{J}=P_{a}$ is defined as the parity of the term $x$. For the spatial spin, we thus have $\sigma_{r}=$ $=i J P_{a}$, so that for the general Straton spin

<!--okf:source lang="de"-->
> **Original**
>
> was wegen $e^{i \varphi}=\cos \varphi+i \sin \varphi$ mit $\cos \left(\frac{\pi}{2}\right)=0$ und $\sin \left(\frac{\pi}{2}\right)=1 \mathrm{zu}$  $\exp (i(\pi J+\varphi))=i e^{i \pi J}$, also $z=i \alpha_{p} e^{i \pi J} \quad$ führt. Es ist $e^{i \pi J}=$  $=\cos (\pi J)+\operatorname{isin}(\pi J)$, während für $J=Q / 2$ gilt. Die positi ven ganzen Zahlen $Q \geqq O$ liefern ganz- oder halbzahlige $J$-Werte.  Im Fall $Q=2 n$ wird $J=n$ ganzzahlig, was $\cos \pi n= \pm 1$ und  $\sin (\pi n)=0$, also $\cos (\pi J)+i \sin (\pi J)= \pm 1$ bedingt. Ist dagegen  $Q=2 n+1$, also $J$ halbzahlig, dann gilt $\cos (\pi J)=0$ und  $\sin (\pi J)=1$, also $\cos (\pi J)+i \sin (\pi J)=i$. Da es für $Q$ keine weiteren  Möglichkeiten gibt, kann $e^{i \pi J}=\cos (\pi J)+i \sin (\pi J)=(-1)^{J}$ gesetzt  werden, was $z(J)=i \alpha_{p}(-1)^{J}$ liefert. Hierin werde $\alpha_{p}(-1)^{J}=P_{a}$ als  Parität des Terms $x$ bezeichnet. Für den Raumspin gilt also $\sigma_{r}=$  $=i J P_{a}$, so daß sich für den allgemeinen Stratonspin
<!--/okf:source-->

. Since $\bar{e}_{t} \perp \bar{e}_{r}$ holds, the following holds for the two components of the Straton spin

<!--okf:source lang="de"-->
> **Original**
>
> ergibt. Da $\bar{e}_{t} \perp \bar{e}_{r}$ gilt, folgt für die beiden Komponenten des Straton spins
<!--/okf:source-->

in which $s$ and $J$, that is, $P$ and $Q$ are always real numbers. However, this  means that $\sigma_{t}$ always remains imaginary, while the algebraic  behavior of $\sigma_{r}$ depends on whether $Q$ is even or odd. $\operatorname{Im}$ In the case $Q=2 n$, $J=n$ becomes an integer, so that $\sigma_{r}= \pm i J= \pm i n$  holds, because $\alpha_{p}= \pm 1$ and, independently of that, $(-1)^{n}= \pm 1$. If the other case $Q=2 n+1$ applies, then $J$ becomes a half-integer, and the real value $\sigma_{r}= \pm J$ follows, because $(-1)^{J}= \pm i$ must be set due to $(-1)^{2 n+1}=-1$ and $\sqrt{-1}=i$. The component $\sigma_{t}$ is therefore  always imaginary, while $\sigma_{r}$ becomes real in the case of a half-integer $J$. It follows from this that $\sigma_{t}$—as the spin of imaginary condenser fluxes in the case of $c$  and $d$ hermetics according to (79), $\sigma_{t}$ represents the Straton field of ponderability as

<!--okf:source lang="de"-->
> **Original**
>
> in denen $s$ und $J$, also $P$ und $Q$ immer reelle Zahlen sind. Dies  bedeutet aber, daß $\sigma_{t}$ stets imaginär bleibt, während das algebraische  Verhalten von $\sigma_{r}$ davon abhängt ob $Q$ geradzahlig oder ungeradzahlig  ist. $\operatorname{Im}$ Fall $Q=2 n$ wird $J=n$ ganzzahlig, so daß $\sigma_{r}= \pm i J= \pm i n$  gilt, weil $\alpha_{p}= \pm 1$ und davon unabhängig $(-1)^{n}= \pm 1$ ist. Liegt der  andere Fall $Q=2 n+1$ vor, dann wird $J$ halbzahlig und es folgt der  reelle Wert $\sigma_{r}= \pm J$, weil $(-1)^{J}= \pm i$ wegen $(-1)^{2 n+1}=-1$  und $\sqrt{-1}=i$ gesetzt werden muß. Die Komponente $\sigma_{t}$ zählt also  stets imaginär, während $\sigma_{r}$ im Fall halbzahliger $J$ reell wird. Hieraus  folgt, daß $\sigma_{t}$ zwar als Spin imaginärer Kondensorflüsse im Fall der $c$  und $d$-Hermetrie nach (79) das Stratonfeld der Ponderabilität als
<!--/okf:source-->

which, because of $e^{i \varphi}=\cos \varphi+i \sin \varphi$, leads to $\cos \left(\frac{\pi}{2}\right)=0$ and $\sin \left(\frac{\pi}{2}\right)=1 \mathrm{zu}$  $\exp (i(\pi J+\varphi))=i e^{i \pi J}$, which leads to $z=i \alpha_{p} e^{i \pi J} \quad$. It is $e^{i \pi J}=$  $=\cos (\pi J)+i \sin (\pi J)$, while the following applies to $J=Q / 2$. The positive integers $Q \geqq O$ yield integer or half-integer $J$ values.  In the case of $Q=2 n$, $J=n$ becomes an integer, which implies $\cos \pi n= \pm 1$ and  $\sin (\pi n)=0$, i.e., $\cos (\pi J)+i \sin (\pi J)= \pm 1$ under certain conditions. If, on the other hand,  $Q=2 n+1$, that is, if $J$ is a half-integer, then $\cos (\pi J)=0$ and $\sin (\pi J)=1$ hold, that is, $\cos (\pi J)+i \sin (\pi J)=i$. Since there are no other possibilities for $Q$, $e^{i \pi J}=\cos (\pi J)+i \sin (\pi J)=(-1)^{J}$ can be set, which yields $z(J)=i \alpha_{p}(-1)^{J}$. Here, $\alpha_{p}(-1)^{J}=P_{a}$ is defined as the  parity of the term $x$. For the spatial spin, we thus have $\sigma_{r}=$ $=i J P_{a}$, so that for the general Straton spin

<!--okf:source lang="de"-->
> **Original**
>
> was wegen $e^{i \varphi}=\cos \varphi+i \sin \varphi$ mit $\cos \left(\frac{\pi}{2}\right)=0$ und $\sin \left(\frac{\pi}{2}\right)=1 \mathrm{zu}$  $\exp (i(\pi J+\varphi))=i e^{i \pi J}$, also $z=i \alpha_{p} e^{i \pi J} \quad$ führt. Es ist $e^{i \pi J}=$  $=\cos (\pi J)+i \sin (\pi J)$, während für $J=Q / 2$ gilt. Die positi ven ganzen Zahlen $Q \geqq O$ liefern ganz- oder halbzahlige $J$-Werte.  Im Fall $Q=2 n$ wird $J=n$ ganzzahlig, was $\cos \pi n= \pm 1$ und  $\sin (\pi n)=0$, also $\cos (\pi J)+i \sin (\pi J)= \pm 1$ bedingt. Ist dagegen  $Q=2 n+1$, also $J$ halbzahlig, dann gilt $\cos (\pi J)=0$ und  $\sin (\pi J)=1$, also $\cos (\pi J)+i \sin (\pi J)=i$. Da es für $Q$ keine weiteren  Möglichkeiten gibt, kann $e^{i \pi J}=\cos (\pi J)+i \sin (\pi J)=(-1)^{J}$ gesetzt  werden, was $z(J)=i \alpha_{p}(-1)^{J}$ liefert. Hierin werde $\alpha_{p}(-1)^{J}=P_{a}$ als  Parität des Terms $x$ bezeichnet. Für den Raumspin gilt also $\sigma_{r}=$  $=i J P_{a}$, so daß sich für den allgemeinen Stratonspin
<!--/okf:source-->

. Since $\bar{e}_{t} \perp \bar{e}_{r}$ holds, the following holds for the two components of the Straton spin

<!--okf:source lang="de"-->
> **Original**
>
> ergibt. Da $\bar{e}_{t} \perp \bar{e}_{r}$ gilt, folgt für die beiden Komponenten des Straton spins
<!--/okf:source-->

in which $s$ and $J$, that is, $P$ and $Q$ are always real numbers. However, this  means that $\sigma_{t}$ always remains imaginary, while the algebraic  behavior of $\sigma_{r}$ depends on whether $Q$ is even or odd. $\operatorname{Im}$ In the case $Q=2 n$, $J=n$ becomes an integer, so that $\sigma_{r}= \pm i J= \pm i n$  holds, because $\alpha_{p}= \pm 1$ and, independently of that, $(-1)^{n}= \pm 1$. If the other case $Q=2 n+1$ applies, then $J$ becomes a half-integer, and the real value $\sigma_{r}= \pm J$ follows, because $(-1)^{J}= \pm i$ must be set due to $(-1)^{2 n+1}=-1$ and $\sqrt{-1}=i$. The component $\sigma_{t}$ is therefore  always imaginary, while $\sigma_{r}$ becomes real in the case of a half-integer $J$. It follows from this that $\sigma_{t}$—as the spin of imaginary condenser fluxes in the case of $c$  and $d$ hermetics according to (79), $\sigma_{t}$ represents the Straton field of ponderability as

<!--okf:source lang="de"-->
> **Original**
>
> in denen $s$ und $J$, also $P$ und $Q$ immer reelle Zahlen sind. Dies  bedeutet aber, daß $\sigma_{t}$ stets imaginär bleibt, während das algebraische  Verhalten von $\sigma_{r}$ davon abhängt ob $Q$ geradzahlig oder ungeradzahlig  ist. $\operatorname{Im}$ Fall $Q=2 n$ wird $J=n$ ganzzahlig, so daß $\sigma_{r}= \pm i J= \pm i n$  gilt, weil $\alpha_{p}= \pm 1$ und davon unabhängig $(-1)^{n}= \pm 1$ ist. Liegt der  andere Fall $Q=2 n+1$ vor, dann wird $J$ halbzahlig und es folgt der  reelle Wert $\sigma_{r}= \pm J$, weil $(-1)^{J}= \pm i$ wegen $(-1)^{2 n+1}=-1$  und $\sqrt{-1}=i$ gesetzt werden muß. Die Komponente $\sigma_{t}$ zählt also  stets imaginär, während $\sigma_{r}$ im Fall halbzahliger $J$ reell wird. Hieraus  folgt, daß $\sigma_{t}$ zwar als Spin imaginärer Kondensorflüsse im Fall der $c$  und $d$-Hermetrie nach (79) das Stratonfeld der Ponderabilität als
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 2](../../sources/bh2.md), page 215.

---
title: "Elementary Structures of Matter, Volume 1 — page 147"
type: page
tags: [BH1org_OCR, page]
sources: [BH1org_OCR]
page: 147
lang: EN-US
lang_source: de
---
Add a function that depends on $p$ integer indices $k_{1}^{(i)} \geqq 1$ with $1 \leqq l \leqq p$ as defined by $n_{i}\left(k_{l}^{(i)}\right)_{1}^{p}$, so that each metron in $R_{p}$ is determined by $p$ as integers $k_{l}^{(i)}$. This assignment law $n_{i}$ within the simple hyperstructures $i$ of the $R_{N}$ (general hyperstructure) is essentially determined by the domain of definition  $P_{l}^{(i)} \leqq k_{l}^{(i)} \leqq Q_{l}^{(i)}$ of these fine-structure numbers, that is, the metric limitation of the simple hyperstructure $i$ in the corresponding $R_{p}$. According to selector theory, $n_{i}\left(k_{l}^{(i)}\right)_{1}^{p}=c_{i} ; n$ could thus be expressed by a selector, the so-called fine-structure selector $c_{i}=c_{i}\left(\kappa_{l}^{(i)}\right)_{1}^{p}$,  , which is in any case a combined functional selector whose $p$-dimensional selector argument $\kappa_{l}^{(i)}$ is always  $p$ coordination selectors, because the $k_{l}^{(i)}$ independently  traverse the integers of their domains of definition  $P_{l}^{(i)} \leqq k_{l}^{(i)} \leqq Q_{l}^{(i)}$. With this fine-structure selector, the concept of the metronic field $\varphi\left(n_{i}\right)_{1}^{L}$ is extended;  for the following holds: $\varphi\left(n_{i}\right)_{1}^{L}=\varphi\left(c_{i} ; n\right)_{1}^{L}=\phi ; n$ with the combined functional selector $\phi=\phi\left(K_{k}\right)_{1}^{G}$, whereby the $G \neq L$ functional selectors  ($G=L$ is also possible), $K_{k}$ the $L$ finestructure selectors $c_{i}$. The finestructures of the simple tensors $R_{p}$ of a $L$-fold hyperstructure in $R_{N}$ are therefore described by

<!--okf:source lang="de"-->
> **Original**
>
> Funktion zu ergänzen, welche von $p$ ganzzahligen Indizes $k_{1}^{(i)} \geqq 1$ mit  $1 \leqq l \leqq p$ gemäß $n_{i}\left(k_{l}^{(i)}\right)_{1}^{p}$ abhängt, so daß jedes Metron im $R_{p}$ durch  $p$ ganze Zahlen $k_{l}^{(i)}$ festgelegt wird. Dieses Zuordnungsgesetz $n_{i}$ in nerhalb der einfachen Hyperstrukturen $i$ des $R_{N}$ (allgemeine Hyper struktur) wird im wesentlichen durch den Definitionsbereich  $P_{l}^{(i)} \leqq k_{l}^{(i)} \leqq Q_{l}^{(i)}$ dieser Feinstrukturziffern, also die metrische Be grenzung der einfachen Hyperstruktur $i$ im zugehörigen $R_{p}$ be stimmt. Nach der Selektortheorie könnte also $n_{i}\left(k_{l}^{(i)}\right)_{1}^{p}=c_{i} ; n$ durch  einen Selektor, den sogenannten Feinstrukturselektor $c_{i}=c_{i}\left(\kappa_{l}^{(i)}\right)_{1}^{p}$  ausgedrückt werden, der auf jeden Fall ein kombinierter Funktional selektor ist, dessen $p$-dimensionales Selektorargument $\kappa_{l}^{(i)}$ immer  $p$ Koordinationsselektoren enthalten muß, weil die $k_{l}^{(i)}$ unabhängig  voneinander die ganzen Zahlen ihrer Definitionsbereiche  $P_{l}^{(i)} \leqq k_{l}^{(i)} \leqq Q_{l}^{(i)}$ durchlaufen. Mit diesem Feinstrukturselektor er fährt der Begriff des metronischen Feldes $\varphi\left(n_{i}\right)_{1}^{L}$ eine Erweiterung;  denn es gilt $\varphi\left(n_{i}\right)_{1}^{L}=\varphi\left(c_{i} ; n\right)_{1}^{L}=\phi ; n$ mit dem kombinierten Funk tionalselektor $\phi=\phi\left(K_{k}\right)_{1}^{G}$, wobei die $G \neq L$ Funktionalselektoren  ( $G=L$ ist auch möglich), $K_{k}$ die $L$ Feinstrukturselektoren $c_{i}$ enthal ten. Die Feinstrukturen der einfachen Tensorien $R_{p}$ einer $L$-fachen  Hyperstruktur im $R_{N}$ werden demnach beschrieben durch
<!--/okf:source-->

Here, the selector $\phi$ is referred to as a field selector because, through  its selection law, it describes the metronic field $\varphi$ in general metronic tensors with respect to its variation. The fine-structure equation (M18) alone cannot yet describe the hyperstructure; for this, a metric analysis is still required. The $L$ simple tensors $n_{i}$ can be geometrically represented in a $N=p M$-dimensional space $R_{N}$ if $L$ satisfies the selection rule $L=\binom{N}{p}$. In the general case, this $R_{N}$ has the metric structure ${ }^{2} \bar{g}\left(x^{\underline{k}}\right)_{1}^{N} \neq{ }^{2} \bar{g} x$, when it is expressed in terms of arbitrary contravariant coordinates $x^{\underline{k}}$. This necessary metric  analysis must, in the most general case, be based on a metronic theory—

<!--okf:source lang="de"-->
> **Original**
>
> Hierin wird der Selektor $\phi$ als Feldselektor bezeichnet, weil er durch  sein Auswahlgesetz das metronische Feld $\varphi$ in allgemeinen metroni schen Tensorien hinsichtlich seines Verlaufes beschreibt. Die Fein strukturgleichung (M18) allein kann noch nicht die Hyperstruktur be schreiben; denn hierfür ist noch eine metrische Untersuchung erforder lich. Die $L$ einfachen Tensorien $n_{i}$ sind in einem $N=p M$  dimensionalen Raum $R_{N}$ geometrisch darstellbar, wenn $L$ der Aus wahlregel $L=\binom{N}{p}$ genügt. Dieser $R_{N}$ hat dabei im allgemeinen Fall  die metrische Struktur ${ }^{2} \bar{g}\left(x^{\underline{k}}\right)_{1}^{N} \neq{ }^{2} \bar{g} x$, wenn er auf beliebige kontrava riante Koordinaten $x^{\underline{k}}$ bezogen wird. Diese notwendige metrische  Untersuchung muß im allgemeinsten Fall auf eine metronische Theo-
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 1](../../sources/BH1org_OCR.md), page 147.

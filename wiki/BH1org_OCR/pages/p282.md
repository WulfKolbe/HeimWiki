---
title: "Elementary Structures of Matter, Volume 1 — page 282"
type: page
tags: [BH1org_OCR, page]
sources: [BH1org_OCR]
page: 282
lang: EN-US
lang_source: de
---
and if $\Delta x_{k}=n_{k} \alpha_{k}$ applies with $\alpha_{k} \sim \delta s_{0}=\sqrt{\tau}$, then $\Delta x_{k} \sim 1 / M_{k}$  with $n_{k}>1$ "energetically" describe a $R_{6}$, provided that $\Delta x_{k} \gg \delta s_{0}$ and $k>6$ hold; however, in general, with respect to volume, a $R_{12}$, for which the volume difference $\left|\Delta V_{12}\right|=\left|\Delta V_{6}\right| \prod_{k=7}^{12}\left|\Delta x_{k}\right|$ applies. Since, according to (3) and  (3a) or (3b), the transition from $R_{4} \rightarrow R_{6}$ to $\varepsilon_{r s} \sim \lambda_{(p)}(k, m) \varphi_{k m}^{(p)}$ decreases in exactly the same way as (3d) and $\varepsilon_{r s}$ for $r \leqq 12$ and (or) $s \leqq 12$, but $\varepsilon_{r s}$  always retains the same dimensions, this must also hold true for $\Delta x_{k}$ with  $k>6$. On the other hand, if we consider the semantics of the Hermetic forms $a$ through $d$ in the context of the Hermetic  $R_{6}$ structure, then $x_{5}$ and $x_{6}$ to refer to the physically weakest field in terms of energy (gravity), which may also be explained $\varepsilon_{r s} \approx 0$ outside the $R_{6}$section in the energy density tensor of $R_{12}$. To continue the investigations,  abstract set theory must now be employed. 

From a power set of a set $M$, that is,  $P(M)=\{\{\emptyset\},\{a\},\{b\},\{c\},\{a, b\},\{a, c\},\{b, c\},\{a, b, c\}\}$, if $a, b$ and $c$  are elements of $M$, set elements from $P$ be selected such that they form a chain, such as, for example,  $\{\emptyset\} \subset\{a\} \subset\{a, b\} \subset\{a, b, c\}$. These chain links form a  chain of subsets; however, for the following considerations, the element $\{\emptyset\}$ is omitted as irrelevant. Thus, a set $T(M)=\{\{a\},\{a b\},\{a b c\}\}$ would exist. If a sequence of elements $a_{i}$ is defined with $1 \leqq i \leqq k$ defined in $N=\left\{a_{1}, \ldots, a_{k}\right\}$, then this corresponds to a vector representation. In this case, in $N$ the following linking rules $o_{j}$ are possible, which connect set elements $a_{i}$ to one another. In such a case, a mapping $N^{k} \rightarrow N$ would take place if $o_{j}\left(a_{1}, \ldots, a_{k}\right)$ holds. $T(M)$ is now a set whose elements are ordered subsets of $M$. If one connects the elements of $T$ and those of the subset of $T$, for example, using operations $o_{j}$ and $o_{k}$, then a mapping of the form $C=a o_{j}\left(a o_{k} b\right) o_{j}\left(a o_{k} b\right) o_{j}\left(a o_{k} b o_{k} c\right)$ would be possible. If $o_{j} \equiv+, o_{k} \equiv+$ is specified, then $C=a+(a+b)+(a+b+c)$ holds.  If $\hat{+}$ is used as the composition operation, then

<!--okf:source lang="de"-->
> **Original**
>
> und gilt $\Delta x_{k}=n_{k} \alpha_{k}$ mit $\alpha_{k} \sim \delta s_{0}=\sqrt{\tau}$, dann würde $\Delta x_{k} \sim 1 / M_{k}$  mit $n_{k}>1$ «energetisch» einen $R_{6}$ beschreiben, sofern $\Delta x_{k} \gg \delta s_{0}$  und $k>6$ ist, jedoch i. B. auf das Volumen einen $R_{12}$, für dessen  Volumendifferenz $\left|\Delta V_{12}\right|=\left|\Delta V_{6}\right| \prod_{k=7}^{12}\left|\Delta x_{k}\right|$ gilt. Da nach (3) und  (3a) bzw. (3b) der Übergang $R_{4} \rightarrow R_{6}$ auf $\varepsilon_{r s} \sim \lambda_{(p)}(k, m) \varphi_{k m}^{(p)}$ eben so zurückgeht wie (3d) und $\varepsilon_{r s}$ für $r \leqq 12$ und (oder) $s \leqq 12$, aber $\varepsilon_{r s}$  stets die gleiche Dimensionierung erhält, muß dies auch für $\Delta x_{k}$ mit  $k>6$ zutreffen. Betrachtet man andererseits die Semantik der Her metrieformen $a$ bis $d$ im Zusammenhang mit der hermetrischen  $R_{6}$-Struktur, dann scheinen sich $x_{5}$ und $x_{6}$ auf das energetisch  schwächste physikalische Feld (Gravitation) zu beziehen, was mögli cherweise ebenfalls $\varepsilon_{r s} \approx 0$ außerhalb des $R_{6}$-Abschnittes im Energie dichtetensor des $R_{12}$ erklärt. Zur Weiterführung der Untersuchungen  muß nunmehr die abstrakte Mengentheorie verwendet werden. 
>
> Aus einer Potenzmenge einer Menge $M$, also  $P(M)=\{\{\emptyset\},\{a\},\{b\},\{c\},\{a, b\},\{a, c\},\{b, c\},\{a, b, c\}\}$, wenn $a, b$ und $c$  Elemente von $M$ sind, können Mengenelemente aus $P$ so ge wählt werden, daß sie eine Kette, wie beispielsweise  $\{\emptyset\} \subset\{a\} \subset\{a, b\} \subset\{a, b, c\}$, bilden. Diese Kettenglieder bilden eine  Kette von Teilmengen, in welcher jedoch für die folgenden Betrachtun gen das Element $\{\emptyset\}$ als nichtrelevant fortgelassen wird. Somit wäre  eine Menge $T(M)=\{\{a\},\{a b\},\{a b c\}\}$ vorhanden. Ist eine Folge  von Elementen $a_{i}$ mit $1 \leqq i \leqq k$ in $N=\left\{a_{1}, \ldots, a_{k}\right\}$ festgelegt, dann  entspricht dies einer Vektordarstellung. In diesem Fall sind in $N$ Ver knüpfungsvorschriften $o_{j}$ möglich, welche Mengenelemente $a_{i}$ mit einander verbinden. In einem solchen Fall würde eine Abbildung  $N^{k} \rightarrow N$ stattfinden, wenn $o_{j}\left(a_{1}, \ldots, a_{k}\right)$ gilt. $T(M)$ ist nunmehr eine  Menge, deren Elemente geordnete Teilmengen von $M$ sind. Wer den die Elemente von $T$ und die der Teilmenge von $T$ beispiels weise durch Operationen $o_{j}$ und $o_{k}$ verknüpft, dann wäre eine Abbil dung der Gestalt $C=a o_{j}\left(a o_{k} b\right) o_{j}\left(a o_{k} b\right) o_{j}\left(a o_{k} b o_{k} c\right)$ möglich. Ist  speziell $o_{j} \equiv+, o_{k} \equiv+$, dann gilt $C=a+(a+b)+(a+b+c)$.  Wird $\hat{+}$ als Verknüpfungsoperation verwendet, dann kann
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 1](../../sources/BH1org_OCR.md), page 282.

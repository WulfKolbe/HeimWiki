---
title: "Elementary Structures of Matter, Volume 1 — page 204"
type: page
tags: [BH1org_OCR, page]
sources: [BH1org_OCR]
page: 204
lang: EN-US
lang_source: de
---
(regarding the $R_{4}$), but $b$ time condensations or $c$ space condensations and $d$ spacetime condensations. One might suspect that  the forms free of condensation stages, $H\left(s_{(3)}\right)$ and $H\left(s_{(4)}\right)$, are the latent potencies of primary self-condensation $a$. If  $a, b, c$ and $d$ turn out to be elementary forms of matter  and if the transitions $a \rightarrow(b, c, d)$ exist, then these powers could be regarded as the propagation of a cosmogony of matter.  Before analyzing these forms $a$ through $d$, it seems sensible to first solve the hermetic fundamental problem (19) in general. 

If the total number of coordinates $q$ that constitute the hermetic architectural units is given, then there are $q=1$ only a finite number of possibilities for condensations (with respect to $x_{5}$ or $x_{6}$), but not a finite number of metric condensation levels. Thus, if there are $1<q \leqq 6$  hermetic coordinates in the $R_{6}$, then it must be possible to  solve (19) for this general hermetic problem $q>1$ of the Hermitian  structure field in such a way that this general solution can be adapted to the respective hermetic structure. The world selector  $L=K-\bar{\lambda} \times()$ allows for a description of the relationship (19) by  the space condenser, namely $K ; \overline{[]}=\bar{\lambda} \times \widehat{[]}$, and this selector relation can in turn be expressed in component form 
$K_{m} ;\left[\begin{array}{cc}i \\ k & l\end{array}\right]=\lambda_{m}(k, l)\left[\begin{array}{cc}i \\ k & l\end{array}\right]$. These $6^{4}=1296$  equations, using the definition of the space condenser—that is, with 
$K_{m} ;\left[\begin{array}{c}i \\ k l\end{array}\right]=\underline{\boldsymbol{\partial}}_{l}\left[\begin{array}{cc}i \\ k & m\end{array}\right]-\underline{\boldsymbol{\partial}}_{m}\left[\begin{array}{cc}i \\ k & l\end{array}\right]+\left[\begin{array}{c}i \\ l s\end{array}\right] ;()\left[\begin{array}{cc}s \\ k & m\end{array}\right]-\left[\begin{array}{cc}i \\ m & s\end{array}\right] ;()\left[\begin{array}{cc}s \\ k & l\end{array}\right] \quad$ as the space condenser—take the form 
$\underline{\mathbf{d}}_{l}\left[\begin{array}{cc}i \\ k & m\end{array}\right]-\underline{\mathbf{d}}_{m}\left[\begin{array}{c}i \\ k\end{array}\right]+\left[\begin{array}{c}i \\ l s\end{array}\right] ;()\left[\begin{array}{cc}s \\ k & m\end{array}\right]-\left[\begin{array}{c}i \\ m\end{array}\right] ;()\left[\begin{array}{c}s \\ k l\end{array}\right]=\lambda_{m}(k, l)\left[\begin{array}{cc}i \\ k & l\end{array}\right] \quad$. Let $q>1$ now be hermetic coordinates. If the covariant components $k$ and $l$ are, respectively, $k$ or $l$ are antihermetic, then $\lambda_{m}(k, l)=0$ follows from the definition of antihermeticity. If, on the other hand, $k$ and  $l$ within the range of the $q$ hermetic coordinates and is $m=\tilde{m}$ anti-

<!--okf:source lang="de"-->
> **Original**
>
> (bezüglich des $R_{4}$ ), aber $b$ Zeitkondensationen bzw. $c$ Raumkonden sationen und $d$ Raumzeitkondensationen. Man könnte vermuten, daß  die von Kondensationsstufen freien Formen $H\left(s_{(3)}\right)$ und $H\left(s_{(4)}\right)$ la tente Potenzen der primären Selbstkondensation $a$ sind. Wenn sich  $a, b, c$ und $d$ als Elementarformen der Materie erweisen sollten  und wenn die Übergänge $a \rightarrow(b, c, d)$ existieren, dann wären diese Po tenzen als Propagation einer Kosmogonie der Materie anzusprechen.  Vor einer Analyse dieser Formen $a$ bis $d$ erscheint es sinnvoll, zu nächst das hermetrische Fundamentalproblem (19) allgemein zu lö sen. 
>
> Ist die Gesamtzahl derjenigen Koordinaten $q$, welche die hermetri schen Architektureinheiten aufbauen, dann gibt es für $q=1$ nur la tente Möglichkeiten von Kondensationen (bezogen auf $x_{5}$ oder $x_{6}$ ),  nicht aber metrische Kondensationsstufen. Wenn es also $1<q \leqq 6$  hermetrische Koordinaten im $R_{6}$ gibt, dann muß es möglich sein,  (19) für dieses allgemeine Hermetrieproblem $q>1$ des hermiteschen  Strukturfeldes zu lösen, derart, daß diese allgemeine Lösung der jewei ligen Hermetriestruktur angepaßt werden kann. Der Weltselektor  $L=K-\bar{\lambda} \times()$ gestattet eine Beschreibung der Beziehung (19) durch  den Raumkondensor, nämlich $K ; \overline{[]}=\bar{\lambda} \times \widehat{[]}$, und diese Selektorbezie hung kann wiederum in die Komponentenform 
> $K_{m} ;\left[\begin{array}{cc}i \\ k & l\end{array}\right]=\lambda_{m}(k, l)\left[\begin{array}{cc}i \\ k & l\end{array}\right]$ gebracht werden. Diese insgesamt $6^{4}=1296$  Gleichungen nehmen unter Verwendung der Definition des Raumkon densors, also wegen 
> $K_{m} ;\left[\begin{array}{c}i \\ k l\end{array}\right]=\underline{\boldsymbol{\partial}}_{l}\left[\begin{array}{cc}i \\ k & m\end{array}\right]-\underline{\boldsymbol{\partial}}_{m}\left[\begin{array}{cc}i \\ k & l\end{array}\right]+\left[\begin{array}{c}i \\ l s\end{array}\right] ;()\left[\begin{array}{cc}s \\ k & m\end{array}\right]-\left[\begin{array}{cc}i \\ m & s\end{array}\right] ;()\left[\begin{array}{cc}s \\ k & l\end{array}\right] \quad$ als Raum kompressor die Form 
> $\underline{\mathbf{d}}_{l}\left[\begin{array}{cc}i \\ k & m\end{array}\right]-\underline{\mathbf{d}}_{m}\left[\begin{array}{c}i \\ k\end{array}\right]+\left[\begin{array}{c}i \\ l s\end{array}\right] ;()\left[\begin{array}{cc}s \\ k & m\end{array}\right]-\left[\begin{array}{c}i \\ m\end{array}\right] ;()\left[\begin{array}{c}s \\ k l\end{array}\right]=\lambda_{m}(k, l)\left[\begin{array}{cc}i \\ k & l\end{array}\right] \quad$ an. Es  seien nun $q>1$ Koordinaten hermetrisch. Sind die kovarianten Kom ponenten $k$ und $l$ bzw. $k$ oder $l$ antihermetrisch, dann folgt  $\lambda_{m}(k, l)=0$ nach der Antihermetriedefinition. Laufen dagegen $k$ und  $l$ im Bereich der $q$ hermetrischen Koordinaten und ist $m=\tilde{m}$ anti-
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 1](../../sources/BH1org_OCR.md), page 204.

---
title: "Elementary Structures of Matter, Volume 1 — page 117"
type: page
tags: [BH1org_OCR, page]
sources: [BH1org_OCR]
page: 117
lang: EN-US
lang_source: de
---
The character $\phi$ is reversed. According to $\partial(u+v)=\partial u+\partial v$ and $\partial C=0$ for $C=$ const, the Metrondifferentiation yields

<!--okf:source lang="de"-->
> **Original**
>
> zeichen von $\phi$ umgekehrt wird. Nach $\partial(u+v)=\partial u+\partial v$ und  $\partial C=0$ für $C=$ const liefert die Metrondifferentiation
<!--/okf:source-->

$\delta_{\boldsymbol{S}}^{n} \varphi(v) \boldsymbol{\partial}_{v}=\lim _{a \rightarrow \mathrm{n}}{\underset{\boldsymbol{S}}{\boldsymbol{S}}}_{a}^{n} \varphi \boldsymbol{\partial}_{v}=\boldsymbol{S}_{\boldsymbol{a}}^{n} \varphi \boldsymbol{\partial}_{v}=\varphi(n) \quad$. The comparison yields $\varphi=\partial \phi$ for metronic integrability, i.e., the metron integral of a function $\varphi$ can always be evaluated if, for $\varphi$, a function $\phi$ such that $\partial \phi=\varphi$ holds, i.e., $\phi$ appears as a primitive function of $\varphi$. Since in 
$\phi(n)-\phi(a-1)=\boldsymbol{S}_{a}^{n} \varphi(v) \boldsymbol{\partial} v \quad$ $\quad \phi(a-1)=C=\mathrm{const} \quad$ is always true because  $a=$ is constant, $\phi(n)=\boldsymbol{S} \varphi(n) \partial n+C$ can be set as a shorthand for the indefinite integral, which, due to $\partial C=0$, again yields the fundamental theorem $\partial \phi=\varphi$ if $\partial \boldsymbol{S} \varphi \partial n=\varphi(n)$ , which is evident from $\boldsymbol{\partial} \boldsymbol{S} \varphi \boldsymbol{\partial} n=\varphi \boldsymbol{\partial} n=\boldsymbol{\varphi}$ and $\boldsymbol{\partial} n=1$. The metronic integrability condition and the fundamental theorem are therefore contained in

<!--okf:source lang="de"-->
> **Original**
>
> $\delta_{\boldsymbol{S}}^{n} \varphi(v) \boldsymbol{\partial}_{v}=\lim _{a \rightarrow \mathrm{n}}{\underset{\boldsymbol{S}}{\boldsymbol{S}}}_{a}^{n} \varphi \boldsymbol{\partial}_{v}=\boldsymbol{S}_{\boldsymbol{a}}^{n} \varphi \boldsymbol{\partial}_{v}=\varphi(n) \quad$ ist. Der Vergleich ergibt  $\varphi=\partial \phi$ für die metronische Integrabilität, d. h., das Metronintegral  einer Funktion $\varphi$ ist immer dann ausführbar, wenn zu $\varphi$ eine Funk tion $\phi$ gefunden werden kann, derart, daß $\partial \phi=\varphi$ ist, also $\phi$ als  primitive Funktion von $\varphi$ erscheint. Da in 
> $\phi(n)-\phi(a-1)=\boldsymbol{S}_{a}^{n} \varphi(v) \boldsymbol{\partial} v \quad$ immer $\quad \phi(a-1)=C=\mathrm{const} \quad$ wegen  $a=$ const ist, kann zur Kürzung für das unbestimmte Metroninte gral $\phi(n)=\boldsymbol{S} \varphi(n) \partial n+C$ gesetzt werden, was wegen $\partial C=0$ wieder  den Fundamentalsatz $\partial \phi=\varphi$ liefert, wenn $\partial \boldsymbol{S} \varphi \partial n=\varphi(n)$ ist, was  wegen $\boldsymbol{\partial} \boldsymbol{S} \varphi \boldsymbol{\partial} n=\varphi \boldsymbol{\partial} n=\boldsymbol{\varphi}$ und $\boldsymbol{\partial} n=1$ evident ist. Die metroni sche Integrabilitätsforderung und der Fundamentalsatz sind demnach  enthalten in
<!--/okf:source-->

This relationship enables the development of metronic integration rules. 

When developing such rules, one can start from the rules of metronic differentiation. For $\partial \varphi=\sum_{j} \partial u_{j}$, 
$\varphi=\boldsymbol{S} \sum_{j} \partial u_{j}=\sum_{j} u_{j}$ holds, which, due to $\boldsymbol{S} \partial u_{j}=u_{j}$, leads to the commutation 
$\boldsymbol{S} \Sigma=\Sigma \boldsymbol{S}$ of the operations. If $\boldsymbol{\partial} \varphi=0$, then only  $\boldsymbol{S} \boldsymbol{\partial} \varphi=C=$ can be const, and for $\boldsymbol{\partial} \varphi=a \boldsymbol{\partial} u$, $\boldsymbol{S} \boldsymbol{\partial} \varphi=\boldsymbol{S} a \boldsymbol{\partial} u$ holds, which, when compared to $\varphi=a u$, becomes const with $a=$.  Finally, the Metron integral can be extended over $\partial(u v)=u \partial v+$ to $+v ð u-{ }^{\partial} u{ }^{\partial} v$. From 
$u v=\boldsymbol{S} u ð v+\boldsymbol{S} v ð u-\boldsymbol{S} ð u ð v$, when $\boldsymbol{\partial}_{v}=f$—that is, $v=\boldsymbol{S} f{ }^{\boldsymbol{o}} n$—is substituted, $\boldsymbol{S} u f{ }^{\mathbf{ð}} n=u \boldsymbol{S} f{ }^{\mathbf{x}} n-\boldsymbol{S}{ }^{\mathbf{x}} u \boldsymbol{S} f{ }^{\mathbf{x}} n+\boldsymbol{S} f{ }^{\mathbf{x}} u=u \boldsymbol{S} f{ }^{\mathbf{x}} n+$ 
$+\boldsymbol{S}\left(f-\boldsymbol{S} f{ }^{ð} \mathfrak{n}\right) \partial_{u} u$. These rules of metronic integration can be summarized in  the following relations:

<!--okf:source lang="de"-->
> **Original**
>
> Dieser Zusammenhang ermöglicht die Entwicklung metronischer Inte grationsregeln. 
>
> Bei der Entwicklung solcher Regeln kann man von den Regeln der  metronischen Differentiation ausgehen. Für $\partial \varphi=\sum_{j} \partial u_{j}$ wird 
> $\varphi=\boldsymbol{S} \sum_{j} \partial u_{j}=\sum_{j} u_{j}$, was wegen $\boldsymbol{S} \partial u_{j}=u_{j}$ zur Vertauschung 
> $\boldsymbol{S} \Sigma=\Sigma \boldsymbol{S}$ der Operationen führt. Ist $\boldsymbol{\partial} \varphi=0$, so kann nur  $\boldsymbol{S} \boldsymbol{\partial} \varphi=C=$ const sein, und für $\boldsymbol{\partial} \varphi=a \boldsymbol{\partial} u$ wird $\boldsymbol{S} \boldsymbol{\partial} \varphi=\boldsymbol{S} a \boldsymbol{\partial} u$, was  mit $\varphi=a u$ verglichen zu $\boldsymbol{S} a \boldsymbol{\partial} u=a \boldsymbol{S} \partial u$ mit $a=$ const wird .  Schließlich kann noch das Metronintegral über $\partial(u v)=u \partial v+$  $+v ð u-{ }^{\partial} u{ }^{\partial} v$ erstreckt werden. Man erhält aus 
> $u v=\boldsymbol{S} u ð v+\boldsymbol{S} v ð u-\boldsymbol{S} ð u ð v$, wenn $\boldsymbol{\partial}_{v}=f$, also $v=\boldsymbol{S} f{ }^{\boldsymbol{o}} n$ einge führt wird, $\boldsymbol{S} u f{ }^{\mathbf{ð}} n=u \boldsymbol{S} f{ }^{\mathbf{x}} n-\boldsymbol{S}{ }^{\mathbf{x}} u \boldsymbol{S} f{ }^{\mathbf{x}} n+\boldsymbol{S} f{ }^{\mathbf{x}} u=u \boldsymbol{S} f{ }^{\mathbf{x}} n+$ 
> $+\boldsymbol{S}\left(f-\boldsymbol{S} f{ }^{ð} \mathfrak{n}\right) \partial_{u} u$. Diese Regeln metronischer Integration können in  den nachstehenden Beziehungen zusammengefaßt werden:
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 1](../../sources/BH1org_OCR.md), page 117.

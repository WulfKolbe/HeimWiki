---
title: "Elementary Structures of Matter, Volume 1 — page 73"
type: page
tags: [BH1org_OCR, page]
sources: [BH1org_OCR]
page: 73
lang: EN-US
lang_source: de
---
The values $\lambda_{p}(k, m)$ of this operator action must then be real and  form discrete point spectra. Here, too, the interpretation  $\operatorname{der} \lambda_{p}$ as discrete structural levels seems to hold; for $C_{p}$, which nonlinearly links the $\left\{\begin{array}{cc}i \\ k & m\end{array}\right\}$, is determined as a functional operator solely by metric quantities, so that the operator action on a metric function of $R_{6}$ can only be a metric state of this $R_{6}$ itself. For this reason, $C_{p}\left\{k^{p} m\right\}=\lambda_{p}(k, m)\left\{\begin{array}{cc}p & m\}\end{array}\right.$ must therefore be set, whereby—due to the macroscopic transition—$C_{p}\left\{{ }_{k}{ }^{p} m\right\} \rightarrow R_{k m}$ and $R_{k m}=\alpha\left(T_{k m}-g_{k m} T / 4\right)$, the super-$\operatorname{gang} \lambda_{p}\left\{k^{p} m\right\} \rightarrow \alpha\left(T_{k m}-g_{k m} T / 4\right) \quad$ must also exist. Thus, even in the $R_{6}$, the discrete structural levels of energy density are equivalent. 

In general, the $C_{p}$ in a form that extends the pseudotensor rank of the mixed-variant $\left\{\begin{array}{cc}i \\ k & m\end{array}\right\}$, such that $C_{p}\left\{\begin{array}{ll}p & m\end{array}\right\}$ corresponds to a matrix trace. If, for the sake of brevity, the entire scheme  of the 216 components $\left\{\begin{array}{c}\cdot i \\ k m\end{array}\right\}$ is introduced as a pseudotensor $\left(\left\{\begin{array}{c}i \\ k m\end{array}\right)_{6}=\widehat{\{ \}}\right.$  and $C=\sum C_{p}$, then $\left.\left.C_{p} \int_{k}{ }^{p}{ }_{m}\right\}=(s p C \widehat{\{ }\}\right)_{k m}$ and  $(s p C \widehat{\{ }\})_{k m} \rightarrow R_{k m}$ would occur during the transition to the macromolecular continuum.  In this reduction, the $\lambda_{p}$ could be interpreted as vector components of $\vec{\lambda}$  in the $R_{6}$, so that  $\left.\lambda_{p}(k, m)\left\{\begin{array}{c}p \\ k\end{array}\right\}=(\vec{\lambda} \widehat{\{ }\}\right)_{k m}$ can also be written in this notation,  which would result in the matrix trace $s p C \widehat{\}}=\vec{\lambda} \widehat{\{ }$; the order-increasing effect of the Hermitian operator could then be represented by

<!--okf:source lang="de"-->
> **Original**
>
> werte $\lambda_{p}(k, m)$ dieser Operatorwirkung müssen dann reell sein und  diskrete Punktspektren bilden. Auch hier erscheint die Interpretation  $\operatorname{der} \lambda_{p}$ als diskrete Strukturstufen gegeben; denn $C_{p}$, der die $\left\{\begin{array}{cc}i \\ k & m\end{array}\right\}$  nichtlinear verknüpft, wird als Funktionaloperator allein von metri schen Größen bestimmt, so daß die Operatorwirkung auf eine metri sche Funktion des $R_{6}$ nur ein metrischer Zustand dieses $R_{6}$ selbst  sein kann. Aus diesem Grunde muß also $C_{p}\left\{k^{p} m\right\}=\lambda_{p}(k, m)\left\{\begin{array}{cc}p & m\}\end{array}\right.$  gesetzt werden, wobei wegen des makromaren Übergangs  $C_{p}\left\{{ }_{k}{ }^{p} m\right\} \rightarrow R_{k m}$ und $R_{k m}=\alpha\left(T_{k m}-g_{k m} T / 4\right)$ auch der Über $\operatorname{gang} \lambda_{p}\left\{k^{p} m\right\} \rightarrow \alpha\left(T_{k m}-g_{k m} T / 4\right) \quad$ existieren muß. Auch im $R_{6}$  sind also die diskreten Strukturstufen Energiedichten äquivalent. 
>
> Im allgemeinen wirken die $C_{p}$ in einer den Pseudotensorgrad  der gemischtvarianten $\left\{\begin{array}{cc}i \\ k & m\end{array}\right\}$ erweiternden Form, so daß $C_{p}\left\{\begin{array}{ll}p & m\end{array}\right\}$  einer Matrizenspur entspricht. Wird zur Kürzung das gesamte Schema  der 216 Komponenten $\left\{\begin{array}{c}\cdot i \\ k m\end{array}\right\}$ als Pseudotensor $\left(\left\{\begin{array}{c}i \\ k m\end{array}\right)_{6}=\widehat{\{ \}}\right.$  und $C=\sum C_{p}$ eingeführt, dann wäre $\left.\left.C_{p} \int_{k}{ }^{p}{ }_{m}\right\}=(s p C \widehat{\{ }\}\right)_{k m}$ und  $(s p C \widehat{\{ }\})_{k m} \rightarrow R_{k m}$ beim Übergang in das makromare Kontinuum.  Die $\lambda_{p}$ könnten bei dieser Kürzung als Vektorkomponenten von $\vec{\lambda}$  im $R_{6}$ aufgefaßt werden, so daß auch  $\left.\lambda_{p}(k, m)\left\{\begin{array}{c}p \\ k\end{array}\right\}=(\vec{\lambda} \widehat{\{ }\}\right)_{k m}$ in dieser Schreibweise gesetzt werden kann,  was zur Matrizenspur $s p C \widehat{\}}=\vec{\lambda} \widehat{\{ }$  sorgrad erweiternde Wirkung des hermiteschen Operators wäre dann  darstellbar durch
<!--/okf:source-->

Here, too, for the same reasons as in the $R_{4}$ subspace,

<!--okf:source lang="de"-->
> **Original**
>
> Auch hier muß aus den gleichen Gründen wie im $R_{4}$-Unterraum
<!--/okf:source-->


## Source

[Elementary Structures of Matter, Volume 1](../../sources/BH1org_OCR.md), page 73.
